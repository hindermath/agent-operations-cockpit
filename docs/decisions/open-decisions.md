# Offene und bestätigte menschliche Decisions / Open and Confirmed Human Decisions

## Offene Decisions / Open decisions

| ID | Reihe / Series | Entscheidung / Decision | Blockiert / Blocks | Frühester Entscheidungszeitpunkt / Earliest point |
|---|---|---|---|---|
| DEC-T02 | RAW-01 | Solution- und Projektzuschnitt; das Testframework ist bereits durch IAD103 bestätigt. / Solution and project layout; the test framework is already confirmed by IAD103. | Produkt-Scaffold / product scaffold | vor erstem Scaffold / before the first scaffold |
| DEC-T04 | RAW-04 | TUI/UI-Framework, Responsiveness und Lokalisierungsformat / TUI/UI framework, responsiveness, and localisation format | Presentation Plan | nach Console/JSON Contract / after the console/JSON contract |
| DEC-T06 | RAW-05 | Node Attestation und Timeout Policy / node attestation and timeout policy | Node Implementation | nach RAW-05 Research / after RAW-05 research |

## Bestätigte und supersedierte Decisions / Confirmed and superseded decisions

| ID | Reihe / Series | Status | Nachweis und Supersession / Evidence and supersession |
|---|---|---|---|
| IAD101 | RAW-01 | Answered | `net10.0` ist als plattformneutraler TFM bestätigt und ersetzt den TFM-Anteil von DEC-T01. / `net10.0` is confirmed as the platform-neutral TFM and supersedes the TFM part of DEC-T01. |
| IAD102 | RAW-01 | Answered | `WorkspaceSnapshot` ist als versioniertes JSON plus JSON Schema bestätigt. / `WorkspaceSnapshot` is confirmed as versioned JSON plus JSON Schema. |
| IAD103 | RAW-01 | Answered | xUnit.net v3 mit Microsoft Testing Platform v2 ist bestätigt; DEC-T02 bleibt nur für Solution-/Projektzuschnitt offen. / xUnit.net v3 with Microsoft Testing Platform v2 is confirmed; DEC-T02 remains open only for solution/project layout. |
| IAD201 | RAW-02 | Answered | RAW-02 besitzt den transportneutralen IPC-/Prozessvertrag; konkrete Process API und Transport bleiben bei RAW-06. / RAW-02 owns the transport-neutral IPC/process contract; concrete process API and transport remain with RAW-06. |
| IAD202 | RAW-02 | Answered | Nur bestätigte Fokus-/Routingauswahl mit Schemaversion darf persistieren; Laufzeitkontext bleibt flüchtig. / Only confirmed focus/routing choices with schema version may persist; runtime context remains volatile. |
| IAD203 | RAW-02 | Answered | Queue-Reihenfolge, Idempotenz, Deduplizierung, Retry und Abbruch sind bestätigt; nicht-idempotente Aktionen werden nicht automatisch wiederholt. / Queue order, idempotency, deduplication, retry, and cancellation are confirmed; non-idempotent actions are not replayed automatically. |
| IAD301 | RAW-03 | Answered | `observed-at` nutzt die UTC-/RFC-3339-Uhr des beobachtenden Execution Nodes, `freshness-as-of` die UTC-/RFC-3339-Uhr des bewertenden AOC-Prozesses und laufende Dauer eine monotone Uhr. / Observed-at and freshness-as-of use their named UTC clocks; elapsed runtime uses a monotonic clock. |
| IAD302 | RAW-03 | Answered | Versionierte Source-/Capability-Profile verwenden das relative Modell `0,5T / T / 2T` und die Klassen `Fresh`, `Aging`, `Stale`, `Expired`, `Unknown`. / Versioned source/capability profiles use the relative model and five freshness classes. |
| IAD303 | RAW-03 | Answered | Confidence ist deterministisch `High`, `Medium`, `Low` oder `Unknown` mit Reason Code, niemals ein Prozentwert. / Confidence is a deterministic reasoned class, never a percentage. |
| IAD601 | RAW-06 | Answered | Eine typisierte, shell-freie Process API trennt Executable und `argv`, verbietet Shell-Auswertung und Interpolation und liefert strukturierte Lifecycle- und Output-Daten. / A typed, shell-free Process API separates executable and `argv`, forbids shell evaluation and interpolation, and returns structured lifecycle and output data. |
| IAD602 | RAW-06 | Answered | Ein normalisiertes Outcome-Vokabular deckt Erfolg, Nonzero Exit, Startfehler, Timeout, Cancellation, Signalbeendigung und fehlende Tools ab; native Exit-/Signaldetails und partieller Output bleiben erhalten. / A normalized outcome vocabulary covers success, non-zero exit, start failure, timeout, cancellation, signal termination, and missing tools while retaining native exit/signal details and partial output. |
| IAD603 | RAW-06 | Answered | Eine versionierte, fail-closed Allowlist gilt je Capability und Node. Eltern-Environments werden nicht implizit geerbt; Secret-Material wird nur referenziert. / A versioned, fail-closed allowlist applies per capability and node. Parent environments are not inherited implicitly; secret material is referenced only. |
| IAD604 | RAW-06/RAW-05 | Answered | Der Vertrag ist transportneutral. SSH v2 ist nur ein optionaler, standardmäßig deaktivierter Referenzadapter mit festen Endpoints, Host-Identity-Prüfung und Key-/Certificate-Authentifizierung; er erteilt keine Remote-Write- oder Process-Authority. / The contract is transport-neutral. SSH v2 is only an optional, disabled-by-default reference adapter with fixed endpoints, host-identity verification, and key/certificate authentication; it grants no remote-write or process authority. |
| IAD701 | RAW-07 | Answered | Eine plattformübergreifende MIDI-Bibliothek liegt hinter einem dünnen herstellerneutralen Adapter; Raw MIDI bleibt außerhalb des Domain Contracts. / A cross-platform MIDI library sits behind a thin vendor-neutral adapter; raw MIDI stays outside the domain contract. |
| IAD702 | RAW-07 | Answered | Die offizielle Elgato-SDK-Bridge bleibt isoliert; TypeScript ist nur bei SDK-Zwang zulässig und nur normalisierte Capability Events verlassen die Bridge. / The official Elgato SDK bridge remains isolated; TypeScript is allowed only when required and only normalised capability events leave the bridge. |
| IAD703 | RAW-07 | Answered | Die erste Referenzwelle enthält genau ein MIDI-Gerät und ein Stream Deck; Xbox bleibt ein separater späterer Adapterkandidat. / The first reference wave contains exactly one MIDI device and one Stream Deck; Xbox remains a separate later adapter candidate. |
| IAD704 | RAW-07 | Answered | Feldtests erfordern versioniertes Lab-Inventar, gerätespezifische Risiko-/Safety-Prüfung, verifizierten Kill Switch, beaufsichtigten Testplan und dokumentierte Freigabe. / Field tests require the named lab, safety, kill-switch, supervision, and approval evidence. |
| IAD801 | RAW-08 | Answered | Kanonische versionierte JSON-Artefakte liegen unter `evidence/workflow/<workflow-id>/`; atomare Veröffentlichung und Recovery binden das letzte vollständig validierte Receipt. / Canonical versioned JSON artifacts use the named workflow-evidence path; atomic publication and recovery bind the last fully validated receipt. |
| IAD802 | RAW-08 | Answered | Ein versioniertes standardisiertes Attestation-Envelope signiert den kanonischen SHA-256-Inhaltshash; versionierte Trust Policy und fail-closed Prüfung sind bindend. / A versioned standard attestation envelope signs the canonical SHA-256 content hash; a versioned trust policy and fail-closed verification are binding. |
| IAD803 | RAW-08 | Answered | Operative Evidence bleibt 90 Tage, Sicherheits- und Fehlschlagevidence zwölf Monate; Projekt-Receipts bleiben für die Projektlebensdauer, Legal Hold setzt Löschung aus und jede Löschung erhält ein Receipt. / Operational evidence remains for 90 days, security and failure evidence for twelve months; project receipts remain for the project lifetime, legal hold suspends deletion, and every deletion receives a receipt. |
| IAD901 | RAW-09 | Answered | Ein Proposal ist erst für ein getrenntes Promotion Review qualifiziert, wenn mindestens zwei reviewte Findings aus mindestens zwei unabhängigen Projekten, positive und negative Evidence, mindestens eine gebundene Retrospektive, Cross-Project-Bewertung, Kompatibilitäts-, Migrations-, Rollback-, Test-, Security-, A11Y- und Dokumentationsevidence sowie null blocking Findings vorliegen. Tatsächliche Promotion bleibt separat. / A proposal becomes eligible only for a separate promotion review after the complete multi-project evidence threshold; actual promotion remains separate. |
| IAD902 | RAW-09 | Answered | Ziel ist zuerst `hindermath/home-baseline`; `github/spec-kit` ist nur bei nachgewiesener Community-Allgemeingültigkeit und innerhalb der vorgeschriebenen seriellen Einzelwarteschlange zulässig. / The first target is `hindermath/home-baseline`; `github/spec-kit` is permitted only for proven community-general applicability and the required serial single-item queue. |
| AUTH-RAW09-PROMOTION | RAW-09 | Answered | Es gibt keine dauerhafte Promotion Authority und keinen automatischen oder administrativen Bypass. Jedes Proposal benötigt eine neue aktuelle menschliche Promotion-Freigabe; ohne sie bleibt es `Deferred`. / There is no standing promotion authority and no automatic or administrative bypass. Every proposal needs a new current human promotion approval; without it, the proposal remains Deferred. |
| DEC-T01 | RAW-01 | Superseded | Der frühere TFM-Anteil ist durch IAD101 ersetzt; kein offener Rest verbleibt. / The former TFM part is superseded by IAD101; no open remainder remains. |
| DEC-T03 | RAW-03 | Superseded | IAD301–IAD303 ersetzen Zeit-, Freshness- und Confidence-Anteil vollständig; kein offener Rest verbleibt. / IAD301-IAD303 fully supersede the time, freshness, and confidence parts; no open remainder remains. |
| DEC-T05 | RAW-08 | Superseded | IAD801–IAD803 ersetzen Persistenz-, Signatur-/Attestation- und Retention-Anteil vollständig; kein offener Rest verbleibt. / IAD801-IAD803 fully supersede persistence, signature or attestation, and retention; no open remainder remains. |

Eine offene Decision ist kein Fehler. Sie blockiert nur die ausdrücklich
genannte Arbeit. Antworten benötigen ein eigenes Decision Receipt und dürfen
nicht aus Bootstrap, Bibliotheksdefaults oder Agentenpräferenz abgeleitet
werden. Eine bestätigte Decision bleibt offenheitsfrei, bis eine ausdrücklich
dokumentierte Supersession sie ersetzt. / *An open decision is not an error. It
blocks only the named work. Answers require their own decision receipt and must
not be inferred from bootstrap defaults, library defaults, or agent preference.
A confirmed decision remains closed until an explicitly documented
supersession replaces it.*
