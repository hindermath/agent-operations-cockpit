# C005-Planungsremediation / C005 Planning Remediation

Run `8b306e28-51eb-4510-afbc-5056b9aee328`, Phase `atomic-surface-remediation`, Branch `004-series-eligibility`, Datum / Date 2026-09-13.

## Disposition und Umfang / Disposition and scope

**C005: auf Planungsebene repariert.** Der Critical-Befund aus dem erhaltenen `analyze-3-audit/report.md` verlangt nach Verfassungsprinzip II die vollständige minimale Skript-/Hilfe-/Manpage-Einheit im ersten Quellencommit. T008 enthält jetzt eine verpflichtende vorgeschaltete Umsetzung und Verifikation der aktuellen T004–T007-Fläche. Ohne positive aktuelle Evidence kein Delivery-Set/Staging/Quellencommit. Alle minimalen Dateien und ihre Verifikation müssen gemeinsam in denselben T008-Quellencommit eingehen. / ***C005: repaired at planning level.** The preserved Critical finding requires the complete minimal script/help/manual unit in the first source commit. T008 now requires completion and verification of current T004–T007 behavior before delivery validation, staging or committing. Commit all minimal files and their verification together.*

Die Einheit umfasst Bash `--help`/`-h`, PowerShell `-Help`, Advanced Function `Test-AocSeriesEligibility` mit vollständiger bilingualer Comment-Help und sicherem Dot-Sourcing, `docs/man/validate-series-eligibility.1` sowie den minimalen Quickstart-Leserpfad, DE-first/EN-second CEFR B2. Sie dokumentiert und prüft aktuelle Parameter, delegierte Fixture-Prüfung, Integration-Wertecheck, Ausgabe/Exits/Fehlerklassen und null Writes/Starts. / *The unit includes all named help/function/manual/reader surfaces, with complete bilingual B2 documentation and verification of current parameters, delegation, integration checking, output/exits/failures and no writes or starts.*

T027 und T036 erweitern nur die bereits gelieferte Einheit für alle sechs Modi und die finale Schnittstelle/Dokumentation. Sie behaupten keine Ersterstellung erforderlicher atomarer Dateien. Neue Verifikation entsteht erst bei autorisierter Umsetzung in `phase-results/atomic-surface-t008-verification.json`, mit realen Quell-/Testhashes, OS/Runtime/Reviewer, Befehlen, unmittelbaren Exits, Ausgaben, Leserprüfung, Funktions-/CLI-Parität, sicherem Import und unveränderten Vorbereitungstests. / *T027 and T036 only expand the existing unit for the final six-mode interface/docs. Future implementation evidence must bind observed execution, reviewed content, source/test hashes and unchanged preparation tests; it is not created or claimed now.*

## Erhaltene Grenzen / Preserved boundaries

T001–T007 bleiben text- und bytegleich `[X]`; insgesamt 7/57, keine neue Task-ID. Code, Tests, alle historischen Blocked-/Red-/Green-/Statistiknachweise, Verfassungen, akzeptierte Quellen, Gate-/Befehlskataloge und der Koordinator-Laufzustand bleiben unverändert. Die sechs Statistikgrenzen T008/T016/T022/T031/T040/T055 und alle bisherigen Transaktionsschritte bleiben erhalten. / *Preserve completed text, code/tests, historical evidence, constitutions, accepted sources, gate/catalog contracts and coordinator state. Keep 7/57 and all six boundaries with their existing transaction steps.*

Modell-Sandboxes dürfen `.git` nicht schreiben. ENV001 wird als reale Ausführungsgrenze erhalten. Der äußere autonome Koordinator führt script-only Git-Checkpoints mit aktueller Autorität aus: beide exakten unstaged Delivery-Set-Prüfungen, exaktes Staging, beide staged Prüfungen mit echten Indexhashes, Quellencommit und Clean-Tree-Nachweis, unveränderte Rendertransaktion sowie gegebenenfalls separat geprüfter Statistik-only-Commit. Kein Validator, Guard oder Sandboxprofil wird verändert. / *Model sandboxes may not write .git. Retain ENV001; the outer coordinator owns script-only exact Git checkpoints under current authority, including both real-index validation surfaces and the unchanged clean-tree statistics transaction. Change no validator, guard or sandbox profile.*

EL-STOP-001 bleibt ein historisch fehlgeschlagener Renderer-Versuch trotz reparierter Planung; EL-STOP-002 bleibt offen. `analyze-3=Blocked` wird nicht umgeschrieben. Tatsächliche atomare Lieferung, T008, native Gesamtmatrix und alle Liefergates bleiben ausstehend. Keine Implementierung, Commit, Render, Push, Merge, Rename, Level-0-/Preset-/Provideränderung oder neuer Lauf in dieser Phase. / *Historical failures remain failures; real atomic delivery and all implementation/delivery gates remain pending. This phase performs only bounded planning and evidence updates.*

## Prüfung und Nachweise / Verification and evidence

- [Konsistenz und Erhaltung / Consistency and preservation](atomic-surface-remediation-validation.json): exakte C005-Disposition, Task-/Artefakt-/Hashbindungen, unveränderte Checkpointfolge und Handoff.
- [Ausführungsprotokoll / Execution record](atomic-surface-remediation-execution.json): reale lokale Prüfungen und unmittelbare Exitcodes. / Actual local checks and immediate exits.
- [AEPS-Receipt](atomic-surface-remediation-aeps-receipt.md): begrenzte lokale Capture-/Handoff-Evidence. / Bounded local capture and handoff.
- [Plan](../plan.md), [Tasks](../tasks.md), [Quickstart](../quickstart.md), [Checkpoint-Vertrag / checkpoint contract](../contracts/statistics-checkpoints.json), [Dokumentationsauswirkung / documentation impact](../contracts/documentation-impact.json).

Genau eine Dokumentationsentscheidung: `UpdateRequired`, CHG004, Owner AOC Repository Owner. Die aktuelle Evidence-Mapping-Zeile weist T008 die Erstlieferung und T027/T036 ausschließlich Erweiterungen zu. Strukturprüfungen und semantische Konsistenzprüfung gelten dieser Planung, nicht zukünftiger Ausführung. / *Retain one documentation decision and map first delivery to T008, later expansion to T027/T036. Structural and semantic planning checks do not prove future execution.*

## Runner-Handoff / Runner handoff

Das Phasenergebnis zählt die eine beauftragte Remediation-Aufgabe: expectedTasks=1, completedTasks=1, Completed nur bei vollständig bestandenen Planungsprüfungen. Der unveränderte Run-State hat weiterhin den vorherigen Taskhash; der Koordinator übernimmt den vorgeschlagenen neuen Hash bei unverändert 7/57 aus dem Konsistenznachweis. Danach benötigt er ein frisches, hashgebundenes `analyze-4` und aktuelle Autorität vor `implement-3` im selben Lauf. Dieses Ergebnis startet weder Analyze noch Implementierung automatisch. / *The phase result counts one bounded planning task only, and reports Completed only after all planning checks pass. The coordinator adopts the proposed task hash with 7/57, then requires a fresh bound analyze-4 and current authority before implement-3 in the same run. No downstream phase starts automatically.*

Die exakte Runner-Ausgabe lautet `.specify/runtime/autonomous-routing/8b306e28-51eb-4510-afbc-5056b9aee328/atomic-surface-remediation.result.json`. Das Profil `codex-frontier-auto` verwendet `--output-last-message {outputFile}`; deshalb ist auch die finale Antwort ausschließlich das JSON nach `autonomous-phase-result-template.json`. Beide Resultatvalidatoren prüfen das Ergebnis mit dem normalisierten Hash dieses Berichts. / *Write the exact configured output and keep the final response identical structured JSON. Both result validators verify the normalized report hash.*
