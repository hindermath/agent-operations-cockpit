# Implementierungsplan: Portfolio-Ownership / Implementation Plan: Portfolio Ownership

**Branch / Branch**: `002-portfolio-ownership` | **Datum / Date**: 2026-08-30 | **Spezifikation / Spec**: [spec.md](spec.md)
**Eingabe / Input**: Geklaerte Spezifikation, formale [Requirements-Checkliste](checklists/requirements.md) mit `44/44` und die drei in `autonomous-run-state.json` hashgebundenen akzeptierten Artefakte. / *Clarified specification, formal `44/44` requirements checklist, and the three accepted artefacts hash-bound in the autonomous run state.*

## Zusammenfassung / Summary

Dieses Feature korrigiert ausschliesslich die lesbare Decision-Aktualitaet in den Zeilen `C-05` bis `C-09` von `requirements/baseline/portfolio-ownership.md`. Der erste Implementierungsschritt ist ein repraesentativer Red/Green-Slice fuer `C-05`: Die aktuelle vermischte Darstellung wird als fehlschlagender Ausgangszustand gebunden; danach werden beantwortetes `IAD604` und offenes `DEC-T06` getrennt dargestellt. Erst nach gruenem Slice folgt der Rollout auf `C-06` bis `C-09`. / *This feature corrects only readable decision currency in rows `C-05` through `C-09` of the portfolio overview. Implementation begins with a representative red/green `C-05` slice that separates answered `IAD604` from open `DEC-T06`; only then does the rule roll out across `C-06` through `C-09`.*

Der maschinenpruefbare Portfoliovertrag, die Decision Map und die vorhandenen Validatoren bleiben validation-only, solange kein neuer reproduzierbarer Driftbefund eine fail-closed Re-Planung verlangt. Es entstehen kein Produktcode, kein RAW-Reihenstart, keine Level-0-Arbeit und keine Preset-Promotion. Aktuelle positive und negative Acceptance-Evidence, First-reader-, A11Y/B2-, Security/Privacy-, Documentation-Impact-, AEPS- und Statistik-Nachweise werden an exakten Pfaden seriell erzeugt. / *The machine contract, Decision Map, and existing validators remain validation-only absent new reproducible drift. No product code, RAW-series start, Level-0 work, or preset promotion is produced. Current positive/negative acceptance, first-reader, A11Y/B2, security/privacy, documentation-impact, AEPS, and statistics evidence is created serially at exact paths.*

Der normale Feature-Kandidat bindet zusaetzlich einen archivbewussten Schema-`1.1`-Lifecycle-Datensatz und einen 14-Ziele-`programmeEvidenceSnapshot` fuer Run `aa60069e-ded5-463f-a737-9b5aa96070c7` auf Branch `002-portfolio-ownership`. Der Feature-002-lokale Python-Core mit verfassungskonformen Bash- und PowerShell-Core-7-Oberflaechen ersetzt nach dem beabsichtigten C-05-bis-C-09-Delta ausschliesslich generische Receipt-Source-Freshness; historische Zielhashes, unveraenderte Receipt-/Review-Bytes, eindeutige aktuelle Ready-Leaves, beide installierten Review-Oberflaechen und Run-/Branch-/Lifecycle-/Stage-Konsistenz bleiben bindend. Man-Page, bilinguale PowerShell-Hilfe, `Test-AocMetaLh02Snapshot` als Advanced Function/Cmdlet, isolierte Tests und Paritaetscheckliste werden gemeinsam geliefert. T053 verifiziert beide Varianten manuell auf dem verfuegbaren macOS-Host, ohne Windows-Evidence zu behaupten. Weil kein vorhandener Workflow die neuen Snapshot-Tests ausfuehrt, ist `.github/workflows/powershell-analysis.yml` ein minimaler erforderlicher Kandidatenpfad: Die Linux-/macOS-/Windows-Matrix muss die Tests und den PowerShell-Peer auf dem exakten reviewten Head ausfuehren; fehlende Windows-Job-/Runner-/Log-/Command-Evidence blockiert Merge fail-closed. Nach Merge und Fast-forward-Sync des normalen Feature-PR erzeugt das vorhandene Bash- oder PowerShell-Rename-Skript auf einem eigenen Branch genau einen byteidentischen `R100`-Commit als letzte Aufgabe der Polish-Phase. Dieser Rename-Head wird separat reviewt und gemergt; erst danach darf ein kausales PostMerge und gegebenenfalls der vorbenannte Drei-Pfad-Closeout entstehen. / *T053 verifies both variants locally on macOS without claiming Windows. The minimal required workflow delta adds exact-head Feature-002 execution to the existing Linux/macOS/Windows PowerShell Static Analysis matrix; missing Windows evidence blocks merge. The terminal rename and causal closeout contract remains unchanged.*

PR #29 bindet den publizierten Statistik-Head `a78a78558459e32ad640c238f5eaf96337a70f83`. Alle 18 Checks sind terminal; 12 bestanden und sechs technische Jobs scheiterten. Beide Homogeneity-Jobs melden den renderer-owned Drift von `docs/scripts/embedded-scripts.md` (`canonical=131`, `embedded=100`). Die drei PR-Matrixjobs verwerfen die synthetische/detached Checkout-Branchidentitaet; Windows meldet zusaetzlich checkoutbedingte Rohbyte-Drift des Targets und einen nicht ausfuehrbaren WSL-Bash-Launcher. `implement-resume-7` ist deshalb bei T076/T077 wahrheitsgemaess `Blocked`; Admin-Bypass ist unzulaessig. Diese Planphase autorisiert ausschliesslich eine neue begrenzte T079-Transaktion fuer spaetere Implementierung: exakter PR-Head-Checkout und fail-closed logischer Branchresolver, normalisierte Targetbindung mit optionalem Git-Blob-Rohbeweis, explizite Git-for-Windows-Bash-Capability, fokussierte Tests/Help/Man-/Quickstart-/Evidence-Anpassung und die konditionale kanonische Regeneration des Embedded-Inventars. / *PR #29 is blocked on six technical exact-head failures. This phase authorizes only the bounded future T079 remediation contract and performs no implementation, generated-output write, Git, or remote action.*

## Technischer Kontext / Technical Context

**Sprache/Version / Language/version**: Markdown/UTF-8 fuer Domain-, Help- und Review-Inhalte; JSON fuer Gate-, Allowlist-, Lifecycle- und kausale Evidence; Python 3 Standardbibliothek fuer den feature-lokalen read-only Snapshot-Core und seine Tests; Bash 5+ und PowerShell Core 7+ fuer gleichwertige kritische CLI-Oberflaechen. Keine Produkt-Runtime. / *Markdown/UTF-8 and JSON plus a standard-library Python snapshot core and equivalent Bash 5+/PowerShell Core 7+ critical CLI surfaces; there is no product runtime.*

**Primaere Abhaengigkeiten / Primary dependencies**: Keine neue Drittanbieter-Dependency. Der Python-Core bleibt vollstaendig standardbibliotheksbasiert; vorhanden sind Python 3, Bash 5+, PowerShell Core 7+, Git, `rg`, `gitleaks`, GitHub CLI erst beim Remote-Closeout, installierte Intake-/Portfolio-/Autonomous-Validatoren und Statistik-Renderer. / *No new third-party dependency. The Python core remains standard-library only; the remaining named tools already exist.*

**Speicherung / Storage**: Git-getrackte repository-relative Markdown- und JSON-Artefakte; temporaere PreMerge-/Execution-Evidence ausserhalb des Repositorys. / *Git-tracked repository-relative Markdown and JSON artefacts; temporary PreMerge/execution evidence outside the repository.*

**Pruefung / Testing**: Ein Red/Green-Semantik-Slice, vierzeiliger Rollout-Abgleich, alle sechs Portfolio-Bash-/PowerShell-Laeufe, generische Review-/Receipt-/`global-ready`-Eingangsgates vor dem Domain-Delta, danach der Feature-002-Snapshot-Core ueber beide neuen CLI-Peers. Positive und fail-closed Tests decken Run, Branch, Stage, exakte Lifecycle-Form, Original-/Archiv-Exklusivitaet, akzeptierte Zielhash-Drift, Receipt-/Review-Bytes, aktive State-Qualifikation, eindeutige Ready-Leaves und den Ausfall jeder installierten Review-Oberflaeche ab. Die T079-Reparatur ergaenzt fokussierte Faelle fuer Ablehnung synthetischer Merge-/detached-CI-Identitaet, Akzeptanz des exakt bewiesenen PR-Heads, LF-/CRLF-Gleichheit bei substantiver Driftablehnung sowie Git-for-Windows-Bash gegen nicht ausfuehrbaren WSL-Launcher. Zusaetzliche Negativfaelle entstehen als temporaere Projektionen, wenn keine eigene getrackte Fixture noetig ist. Reale Windows-Ausfuehrung bleibt fuer den exakten reviewten Head durch den aktualisierten `PowerShell Static Analysis`-Matrixjob verpflichtend; Linux/macOS bleiben dort ebenfalls Pflicht. Hinzu kommen beide Script-Reference-Preview-/Check-Paare, Series-Aufloesung, Run-State- und Delivery-Set-Peers, unabhaengige Reviews, Statistik-Renderer, exakte Stage, ein byteidentischer `R100`-Commit und kausale Schema-2.0-Evidence. / *T079 adds exact PR-head, synthetic/detached rejection, line-ending equivalence with substantive-drift failure, and executable Windows Bash capability tests while retaining every prior gate.*

**Zielplattform / Target platform**: Text-first Repository-Nutzung auf macOS, Linux und Windows; lokale Referenzausfuehrung auf macOS mit Bash und PowerShell 7, CI-Matrix auf dem explizit ausgecheckten Push-/PR-Head. Windows-Bash-Evidence verwendet einen validierten ausfuehrbaren Git-for-Windows-Pfad; ein WSL-Launcher ohne Distribution ist keine Capability. / *Text-first use on macOS, Linux, and Windows; CI binds the explicit event head and Windows uses executable Git-for-Windows Bash rather than an unavailable WSL launcher.*

**Projekttyp / Project type**: Reine Level-2-Dokumentations- und Governance-Aenderung. / *Documentation-and-governance-only level-2 change.*

**Leistungsziele / Performance goals**: Fuer Produkt und Runtime `N/A`; das ausfuehrbare Validierungstooling besitzt keine Produkt-SLA. Re-Evaluation bei Produkt-, Runtime- oder belastbarem Validator-Performancebedarf. / *`N/A` for product/runtime; the executable validation tooling has no product SLA.*

**Constraints**: DE-first/EN-second, etwa CEFR B2, keine vorausgesetzte Spec-Kit-Erfahrung, WCAG 2.2 AA soweit anwendbar, vollstaendige Textalternativen, Public-Datenklassifikation, keine Secrets/private Pfade/unnoetigen Personendaten, fail-closed bei Scope-, Pfad-, Hash-, Evidence-, Review- oder Authority-Drift. / *German first, equivalent English second, approximately CEFR B2, no assumed Spec Kit experience, WCAG 2.2 AA where applicable, complete text alternatives, Public data only, and fail-closed drift handling.*

**Umfang / Scale/scope**: Fuenf Decision-Zellen mit einem ersten Slice; neun Reihen, neun Concerns, zehn Handoffs; drei aktuelle offene, 23 beantwortete und drei supersedierte Decisions als validation-only Bezugsmenge; ein Lifecycle-Datensatz mit 14-Ziele-Snapshot, ein feature-lokaler Python-Core mit Bash-/PowerShell-Paar, Man-Page, Help/Cmdlet, isolierten Tests/Fixtures und Paritaetsevidence sowie ein spaeterer byteidentischer Rename; 40 deklarierte Gate-Zeilen (`33 Applicable`, `7 N/A`). / *The scope adds one immutable fourteen-target snapshot and a constitution-compliant isolated feature-local validation contract to the documentary feature.*

Es bestehen keine `NEEDS CLARIFICATION`-Marker. / *No `NEEDS CLARIFICATION` marker remains.*

## Verfassungspruefung / Constitution Check

*Gate vor Phase 0: bestanden. Erneute Pruefung nach Phase 1: bestanden. / Gate before Phase 0: passed. Re-check after Phase 1: passed.*

Owner ist der jeweils genannte Fach-Owner; Reviewer ist eine davon unabhaengige Rolle. Jede Zeile trennt Anwendbarkeit und Umsetzungsstatus. Ausfuehrungsevidence bleibt bis zur spaeteren Phase offen, ohne als Klaerungsbedarf zu gelten. / *The named domain owner owns each row and a separate role reviews it. Applicability and implementation status remain separate. Later execution evidence may remain pending without becoming a clarification gap.*

| Pruefpunkt / Checkpoint | Anwendbarkeit / Applicability | Umsetzung / Implementation | Begruendung und Evidence / Rationale and evidence | Owner, Restluecke und Neubewertung / Owner, residual gap, and re-evaluation |
|---|---|---|---|---|
| Level-2-Umgebung / Level-2 environment | N/A | Not Assessed | Das gemeinsame Register in `.specify/memory/constitution.md` enthaelt keine AOC-Zeile; aus dem dokumentarischen Scope mit feature-lokalem Validierungstooling wird keine Produkt-Runtime abgeleitet. AGENTS-Grenzen, Statistik-Konfiguration und aktuelle Workflows liefern den lokalen Kontext. / The shared registry has no AOC row; no product runtime is inferred from documentary scope plus feature-local validation tooling. | Governance Owner; keine Scope-Luecke; neu bei Registereintrag oder Runtime-Handoff. / Re-evaluate for a registry row or runtime handoff. |
| Primaersprache, MSL und sichere Codeerzeugung / Primary language, MSL, and secure code generation | Applicable fuer den feature-lokalen Validator | Partly Fulfilled | Python 3 nutzt nur die Standardbibliothek, read-only Repositoryzugriffe, feste repository-relative Pfade, `subprocess.run` ohne Shell und fail-closed JSON-/Hash-/Stage-Pruefung; installierte/shared Validatoren bleiben unveraendert. / Standard-library Python, no shell interpolation, read-only paths, and fail-closed validation. | Security Owner; Code-/Fixture-Review und Tests stehen aus; neu bei Dependency, Write-Pfad oder Interface-Delta. / Review and tests pending. |
| NIST SSDF | Applicable | Partly Fulfilled | Hashgebundene Eingaben, minimale ausfuehrbare und Schreibflaeche, geschuetzte Quellen, Red/Green-Proof, sichere Produktion, negative Faelle, Reviews, respond/fail-closed Trigger und exakte Delivery-Gates decken den dokumentarischen SDLC und das Validierungstooling ab. / Hash-bound inputs, minimal executable/no-write scope, negative cases, reviews, and fail-closed delivery gates cover the documentary SDLC and validation tooling. | Security Owner; echte T052/T053- und Closeout-Evidence steht aus; neu bei jeder Drift. / Execution evidence remains pending; re-evaluate on drift. |
| CWE Top 25 und sichere Codeerzeugung / CWE Top 25 and secure code generation | Applicable | Partly Fulfilled | Python validiert JSON, Hashes, Pfade und Subprozesse ohne Shell/dynamische Ausfuehrung; Bash verlangt `set -euo pipefail`, Quoting und `--`; PowerShell verlangt `#Requires -Version 7`, `Set-StrictMode -Version Latest`, validierte Parameter, `-NoProfile`, Help und Cmdlet. Secret/Public-Content, Standardbibliothek ohne neue Dependency, negative Faelle, null Write und Paritaet werden reviewt. / Python, Bash, and PowerShell carry the named secure-code, public-content, dependency-free, negative-case, no-write, and parity obligations. | Security Owner; Code-/Fixture-/Parity-Review und manuelle Plattformtests stehen aus. / Code, fixture, parity, and manual platform evidence remains pending. |
| Public-Content-, Secret- und Privacy-Grenze / Public-content, secret, and privacy boundary | Applicable | Partly Fulfilled | Datenklasse `Public`; `gitleaks`, beide Repository-Scanner und ein unabhaengiges per-path Review sind geplant. / Public data only; machine scans and independent per-path review are planned. | Security/Privacy Owner; Ausfuehrung steht aus; neu bei jedem Pfad- oder Inhaltsdelta. / Execution pending; re-evaluate on any content delta. |
| OWASP ASVS | N/A | Not Assessed | Kein Web-, API-, HTTP-, Authentifizierungs- oder Autorisierungsdienst. / No web/API/auth service. | Security Owner; neu bei solcher Schnittstelle. / Re-evaluate for such an interface. |
| SBOM, VEX, SLSA und OpenSSF Scorecard | N/A | Not Assessed | Keine neue Dependency, Komponente, Build-, Paket-, Release- oder Distributionseinheit. / No new dependency or releasable artefact. | Supply-chain Owner; neu bei Dependency-/Build-/Release-Handoff. / Re-evaluate at such a handoff. |
| AI-SBOM | N/A | Not Assessed | KI ist Entwicklungswerkzeug; kein Modell, Dataset, Inferenzdienst oder AI-Runtime-Bestandteil wird ausgeliefert. / AI is development tooling only. | AI Governance Owner; neu bei AI-Produkt-/Runtime-Komponente. / Re-evaluate for an AI component. |
| Sichere Architektur, STRIDE/CIA/CAPEC, S-ADR und arc42 / Secure architecture set | N/A | Not Assessed | Keine Produktstruktur, neue Trust-Grenze, Datenfluss-, Runtime- oder Deployment-Aenderung; die kleine feature-lokale CLI-Oberflaeche bleibt unter Secure-code- und Paritaetsgates und aendert die fachliche Owner/Consumer-Grenze nicht. / No product structure, trust, runtime, or deployment change; the local CLI is bounded by secure-code and parity gates. | Architecture Security Owner; neu bei Produkt-/Trust-/Deployment-Aenderung. / Re-evaluate on such a change. |
| Zero Trust, BSI C3A und BSI C5 | N/A | Not Assessed | Kein verteilter Dienst, Remote-Management, Cloud-Service, SaaS/PaaS/IaaS oder providerabhaengiges Deployment. / No distributed or cloud service. | Cloud Governance Owner; neu bei Cloud-/Remote-Scope. / Re-evaluate on cloud/remote scope. |
| NIS2, CRA, EU AI Act und DORA | N/A | Not Assessed | Keine Marktbereitstellung, Kundenauslieferung, regulierte Lieferkette, AI-Runtime oder Finanzsektor-ICT. / No regulatory trigger. | Regulatory Owner; neu bei Distribution oder reguliertem Kontext. / Re-evaluate on a trigger. |
| OWASP SAMM, Cheat Sheets und Proactive Controls | N/A | Not Assessed | Keine programmweite Reifegradbewertung; die begrenzte Validierungslogik wird stattdessen durch NIST SSDF, CWE Top 25 und die sprachspezifischen sicheren Codepflichten abgedeckt. / No programme-wide maturity assessment; the bounded validation logic is covered by SSDF, CWE, and language-specific secure-code duties. | Security Owner; neu bei separatem Assessment. / Re-evaluate for a separate assessment. |
| Security-Dateien unter `docs/security/` / Security files under `docs/security/` | N/A | Not Assessed | Die feature-lokale Security-/Privacy-Evidence deckt den einzigen anwendbaren Public-Content-Scope; neue globale Security-Dateien waeren unbelegter Scope-Zuwachs. / Feature-local evidence covers the only applicable boundary. | Security Owner; neu bei Trust-, Dependency-, Cloud-, Release- oder Risikoaenderung. / Re-evaluate on such a change. |
| Fachlicher Systemkontext und Interfaces / Domain system context and interfaces | Applicable | Partly Fulfilled | Neun Owner, zehn Handoffs und Consumer-Grenzen bleiben vollstaendig textlich und maschinenpruefbar; nur Decision-Statusdarstellung wird korrigiert. / Owners, handoffs, and consumer boundaries remain text-complete; only status presentation changes. | Portfolio Architecture Owner; sechs Checks und semantisches Review stehen aus. / Six checks and semantic review remain pending. |
| Architekturziele und Qualitaetsszenarien / Architecture goals and quality scenarios | Applicable | Partly Fulfilled | Eindeutigkeit, Azyklizitaet, Fail-closed-Verhalten, Decision-Aktualitaet und Verstaendlichkeit besitzen messbare Szenarien. / The named quality attributes have measurable scenarios. | Portfolio Architecture Owner; Execution Evidence aus Slice, Fixtures und Reviews steht aus. / Execution evidence remains pending. |
| WCAG 2.2 AA und Text-first / WCAG 2.2 AA and text first | Applicable | Partly Fulfilled | Matrix, Handoffs, Status, Decisions und Authority-Grenzen bleiben als Text lesbar; ein unabhaengiges Review muss null Blocker melden. / All meaning remains text-readable; independent review must report zero blockers. | Accessibility Owner; `accessibility-review-evidence.md` steht aus; neu bei jeder Textaenderung. / Evidence pending; re-evaluate for every text change. |
| DE-first/EN-second, CEFR B2 und First reader | Applicable | Partly Fulfilled | Planungsartefakte sind inline bilingual; Fachbegriffe werden bei Erstgebrauch erklaert; kein Spec-Kit-Vorwissen. `6/6` First-reader-Evidence bleibt Gate. / Planning artefacts are bilingual and no prior Spec Kit knowledge is assumed. | Documentation Owner; beide unabhaengigen Reviews stehen aus. / Both reviews remain pending. |
| Plattformparitaet / Platform parity | Applicable | Partly Fulfilled | Der gate-kritische Core besitzt gleichwertige Bash- und PowerShell-Core-7-Oberflaechen. PR #29 beweist, dass reine `bash`-Pfadauflosung auf Windows faelschlich den WSL-Launcher waehlen kann. T079 muss Git-for-Windows-Bash explizit validieren oder bei fehlender Capability fail closed stoppen; kein Peer darf still uebersprungen werden. Reale Ubuntu-/macOS-/Windows-Ausfuehrung bleibt fuer den exakten reviewten Head verpflichtend. / PR #29 proves that path lookup can select unusable WSL Bash; T079 must select executable Git-for-Windows Bash or fail closed while preserving all three platform jobs. | Platform Owner; fokussierte Capability-Tests und neue exakte Workflow-/Job-/Runner-/Log-/Command-Evidence stehen aus. / Focused capability tests and renewed exact-head evidence remain pending. |
| Neues Skript, Man-Page, Cmdlet und Hilfe / New script, man page, cmdlet, and help | Applicable | Partly Fulfilled | T052 lieferte im selben Kandidaten den Python-Core, `contracts/validate-meta-lh02-snapshot.sh`, `contracts/validate-meta-lh02-snapshot.ps1` und `docs/man/validate-meta-lh02-snapshot.1`; PowerShell definiert das genehmigte Advanced Function/Cmdlet `Test-AocMetaLh02Snapshot` mit vollstaendiger bilingualer Comment Help. Beide Peers besitzen Help-Verhalten und sind immer read-only, daher gibt es keinen separaten mutierenden Dry-run. T053 prueft strikte Bash-/PowerShell-Regeln, Help, Get-Help, Cmdlet, Man-Page, Negativ-Fixtures, gleiche Ausgabe/Exitcodes, null Write, vollstaendige lokale Testsuite und `checklists/snapshot-tooling-parity.md` manuell auf macOS. Echte Windows-Evidence folgt fail-closed fuer den exakten reviewten Head. / T052 delivered the paired unit; T053 verifies both variants locally on macOS and exact-head CI later proves Windows. | Platform Owner; formaler T053-Resume und exakte CI-Evidence stehen aus. / Formal T053 resume and exact-head CI evidence remain pending. |
| Agentenparitaet und Templates / Agent parity and templates | N/A | Not Assessed | Keine Aenderung an den fuenf AOC-Agentenflaechen, Templates oder Constitution. / No shared guidance change. | Agent Guidance Owner; bei nachgewiesenem Bedarf fail-closed neu planen und alle fuenf Flaechen atomar behandeln. / Replan all five surfaces if triggered. |
| Installierte Presets / Installed presets | Applicable als Governance-Rahmen | Partly Fulfilled | Security, Architecture, iSAQB, A11Y, Cross-platform und Autonomous Gates sind abgebildet. Intake-Presets liefern aktuelle Eingaben. Model Routing und Parallel Autonomous sind feature-seitig N/A. / Applicable preset rules are mapped; intake presets provide inputs; model routing and parallel campaign are out of feature scope. | Governance Owner; unabhaengiges Plan-Review bestanden, nachgelagerte Gate-Evidence steht aus. / Independent plan review passed; downstream evidence remains pending. |
| Parallel-Ausfuehrung und RAW-Reihen / Parallel execution and RAW series | N/A | Not Assessed | Serielle Shared Writer, ein Feature, kein Worker-DAG und kein RAW-Start. / Serial shared writers, one feature, no RAW start. | Autonomous Run Operator; neu nur bei ausdruecklicher separater Authority. / Re-evaluate only with explicit separate authority. |
| Projektstatistik / Project statistics | Applicable | Partly Fulfilled | Die historische Projektion, der verbrauchte Amend und der publizierte finale Statistik-Head `a78a78558459e32ad640c238f5eaf96337a70f83` bleiben wahrheitsgemaesse Evidence. Die technische T079-Reparatur veraendert den Head und erzwingt deshalb einen neuen T059-Fixpunkt, einen neuen normalen Reparatur-Head und eine neu gerenderte Methodik-v2-Bindung; ein finaler Ledger-only-Synchronisationshead folgt nur, soweit der unveraenderte Renderer ihn weiterhin verlangt. / Preserve the published failed statistics head as history and create a fresh T059 fixed point, normal repair head, and methodology-v2 binding for every new head. | Statistics Maintainer; neuer Dual-`CURRENT`/`0`-Beleg und vollstaendige Head-/PR-Neubindung stehen aus. / Renewed dual CURRENT proof and full head/PR rebinding remain pending. |
| Documentation Impact | Applicable | Partly Fulfilled | Genau eine Entscheidung: `UpdateRequired`; Source, Owner, Leserpfad, Dokumente, Sprache, Plattform, Distribution, Home-sync-N/A, Evidence und Trigger sind geplant. / Exactly one complete `UpdateRequired` decision is planned. | Documentation Owner; konkrete Evidence gegen finalen Kandidaten steht aus. / Candidate-bound evidence remains pending. |
| AEPS-Rueckfuehrung / AEPS feedback | Applicable | Partly Fulfilled | Das Implementierungsassessment aus T054 ist als unabhaengig validiertes `NoChange` erhalten. Die materielle Retrospektive T092 loest nach T092 und vor terminaler Completion in T093 eine abschliessende Finding-oder-NoChange-Neubewertung unter dem bestehenden Repository-AEPS-Vertrag aus. / The independently validated implementation NoChange remains historical; the material retrospective triggers one final assessment after T092 and before terminal completion. | AEPS Evidence Owner und unabhaengiger AEPS Reviewer; keine Level-0-, Promotion-, Upstream- oder Folgelauf-Authority. / Final reassessment pending without upstream or next-run authority. |
| Autonomous `MergeAndSync`, Exact Head und Schema 2.0 | Applicable | Partly Fulfilled | Run-State ist aktiv in der begrenzten Plan-Remediation. `implement-resume-7` ist nach 72/93 Abschluessen bei T076/T077 auf PR #29 und Head `a78a78558459e32ad640c238f5eaf96337a70f83` wahrheitsgemaess `Blocked`: 12/18 Checks bestanden, sechs technische Jobs scheiterten. Kein Merge oder Bypass erfolgte. / The run truthfully records implement-resume-7 blocked at T076/T077 on PR #29 exact head after 72 of 93 tasks. | Autonomous Run Operator; neue ausdrueckliche T079-Implementierungsautoritaet, Fixpunkt, Statistik-/PR-Neubindung und komplette Providerkonvergenz stehen aus. / A new explicit T079 implementation transaction and full reconvergence remain pending. |
| Terminaler Lastenheft-Rename / Terminal intake rename | Applicable | Not Fulfilled | Der normale Kandidat plant `intake-lifecycle.json`; nach seinem Merge erzeugt das bestehende Bash- oder PowerShell-Skript als letzter Polish-Task genau einen byteidentischen `R100`-Commit. Receipt, Review, Series und Lifecycle bleiben unveraendert und werden archivbewusst validiert. / The normal candidate plans the lifecycle record; the separately reviewed rename follows its merge as the final Polish task. | Intake Lifecycle Owner; Lifecycle-Datensatz, Rename-Commit, archivbewusste Gates und eigener Exact-head-Merge stehen aus. / Lifecycle record, rename, archive-aware gates, and separate merge remain pending. |
| Enger Bypass / Narrow bypass | N/A | Not Assessed | PR #29 besitzt sechs technische Exact-head-Fehler. `PO-N01` verbietet Admin-Bypass ausdruecklich, solange irgendein technisches, Security-, Scope-, Evidence-, Review- oder Plattformgate fehlt oder scheitert. / Six technical failures make admin bypass prohibited. | Repository Owner; nur nach vollstaendiger neuer Gruen-Konvergenz und genau einem verbleibenden Approval-/Ruleset-Gate neu bewerten. / Re-evaluate only after complete green convergence and one sole policy blocker. |

### Dokumentationswirkung / Documentation Impact

- **Entscheidung / Decision**: `UpdateRequired`.
- **Kanonische fachliche Quelle / Canonical domain source**: `requirements/intakes/active/Lastenheft_META-LH-02-Portfolio-Ownership.md`, gebunden durch aktuelles Receipt und Ready-Single-Review. / *The bound active META-LH-02 intake.*
- **Primaeres Aktualisierungsziel / Primary update target**: `requirements/baseline/portfolio-ownership.md`, nur Decision-Zellen `C-05` bis `C-09`. / *Only the named decision cells.*
- **Lifecycle-Ableitung / Lifecycle derivation**: `specs/002-portfolio-ownership/intake-lifecycle.json` wird im normalen Kandidaten erzeugt; der spaetere Rename verschiebt das byteidentische Lastenheft vom logischen Originalpfad nach `requirements/intakes/active/Lastenheft_META-LH-02-Portfolio-Ownership.002-portfolio-ownership.md`. Receipt, Review und Series bleiben unveraenderte historische Bindungen. / *The normal candidate creates the lifecycle record; the later byte-identical path transition preserves immutable evidence bindings.*
- **Validation-only**: `requirements/baseline/portfolio-ownership.json`, `docs/decisions/open-decisions.md`, `specs/intake-review-fixtures/meta-lh-02/*`; Aenderung nur nach neuem reproduzierbarem Drift und Re-Planung. / *Change only after new reproducible drift and replanning.*
- **Zielgruppen / Audiences**: IHK-IT-Auszubildende ab Jahr 1, erfahrene Fachkraefte, Reviewer und agentisch arbeitende Maintainer. / *First-year apprentices, professionals, reviewers, and agent-assisted maintainers.*
- **Leserpfad / Reader path**: `spec.md` -> Begriffe -> Portfolio-Uebersicht -> Maschinenvertrag/Decision Map fuer Vertiefung -> Glossar -> naechste sichere Aktion: nur Owner-gebundene Aenderung oder fail-closed Stop. / *Spec to terms, overview, deeper contracts, glossary, and one safe next action.*
- **Navigation / Navigation**: Bestehende Links und Rueckverweise pruefen; kein neuer globaler Einstieg. / *Check existing links; no new global entry.*
- **Dokumentklasse und Sprache / Document class and language**: oeffentliche repository-interne Governance; inline DE-first/EN-second, CEFR B2, keine Partnerdatei. / *Public repository-local governance, inline bilingual.*
- **Plattform-/Beispielnachweis / Platform/example proof**: sechs Bash-/PowerShell-Portfolio-Laeufe plus text-first Reviews. / *Six paired checks plus text-first reviews.*
- **Distribution und Sync / Distribution and sync**: repository-intern; kein Home Sync, Level-0-Handoff oder Preset-Promotion. / *Repository-local only.*
- **Generierte Ableitung / Generated derivation**: `docs/project-statistics.md` nur aus `docs/project-statistics.config.json` und Git-Historie; `docs/scripts/embedded-scripts.md` nur durch `render-script-reference.*`, konditional ausgeloest durch den in-scope Skriptinventar-Delta. Beide werden nie hand-editiert. / *Statistics and the conditionally triggered embedded-script inventory remain renderer-owned outputs.*
- **Evidence / Evidence**: `documentation-impact-evidence.md`, First-reader-/A11Y-/Security-Reviews, Implementierungscheckliste, AEPS-Receipt, Statistik-Checks und spaetere PR-Evidence. / *Named evidence surfaces.*
- **Neubewertung / Re-evaluation**: Concern-, Owner-, Handoff-, Decision-, Pfad-, Hash-, A11Y-, Sprach-, Security-, Review-, Receipt-, Series-, CI- oder Authority-Drift. / *Any named drift.*

### Post-Design-Recheck / Post-design re-check

[research.md](research.md) loest alle technischen Unbekannten auf. [data-model.md](data-model.md) bindet Decision-Status und Evidence-Entitaeten. [quickstart.md](quickstart.md), [Planungs- und Validierungsvertrag](contracts/planning-validation-contract.md), [Delivery-Allowlist](contracts/delivery-allowlist.json) und [Gate-Requirements](autonomous-run-gate-requirements.json) bilden Befehle, Plattformtoken, Evidence-Pfade, N/A-Trigger und serialisierte Shared Writer ab. Keine Verfassungsverletzung oder offene Planungsunklarheit verbleibt. / *The supporting artefacts resolve every unknown and bind commands, tokens, evidence paths, N/A triggers, and serial shared writers. No constitutional violation or planning ambiguity remains.*

### Unabhaengiges Plan-Review / Independent plan review

Das unabhaengige Review und Analyze-11 bewahrten die drei akzeptierten Hash-Bindungen, Scope, Lifecycle, alle 93 Task-IDs und `40 = 33 Applicable + 7 N/A`. Danach wurden ein normaler Amend und der urspruengliche Ledger-only-Statistik-Sync verbraucht; der publizierte finale Statistik-Head `a78a78558459e32ad640c238f5eaf96337a70f83` existiert auf `origin/002-portfolio-ownership` und PR #29. `implement-resume-7` schloss 72 von 93 Tasks ab und stoppte wahrheitsgemaess bei T076/T077: alle 18 Checks terminal, 12 erfolgreich, sechs technische Fehler. Zwei Homogeneity-Jobs melden Embedded-Inventar-Drift; drei PR-Matrixjobs verwerfen die Checkout-Branchidentitaet, Windows zusaetzlich Target-Rohbytes und WSL-Bash. Diese Planungsphase fuehrt keinen Renderer-, Generated-Output-, Test-, Stage-, Commit-, Push-, PR-, CI-, Merge-, Bypass- oder Provider-Schritt aus und markiert keine Aufgabe. Sie bindet nur die spaetere neue T079-Transaktion, den T059-T073-Fixpunkt, einen neuen realen Feature-/Statistik-Head, PR-#29-Neubindung, alle lokalen Gates, alle 18 Providerchecks und vollstaendige Ubuntu-/macOS-/Windows- sowie Review-Evidence. / *Implement-resume-7 truthfully stopped at T076/T077 on PR #29 exact head after six technical failures. This phase is plan-only and authorizes only the future bounded T079 transaction and complete fixed-point reconvergence.*

## Umsetzungsstrategie / Implementation Strategy

### Phase A: Vor-Implementierungsvertraege und Review / Pre-implementation contracts and review

1. Gate-Requirements strukturell und semantisch mit JSON-Parser, installierter Core-Funktion und unabhaengigem Feldabgleich pruefen; Ergebnis sowie identischen normalisierten/rohen SHA-256 dokumentieren, ohne T052/T053-Ausfuehrung zu behaupten. / Validate gate requirements structurally and semantically and record the exact normalized/raw hash without claiming future T052/T053 execution.
2. `contracts/delivery-allowlist.json`, alle `33 Applicable`-/`7 N/A`-Zeilen, Commands, Runner-/Plattformtoken, Owner, Reviewer, Evidence-Pfade und Trigger unabhaengig reviewen. / Independently review all declared fields.
3. `plan-review` muss Gate-Datei und Allowlist explizit akzeptieren; ohne Akzeptanz keine Tasks und kein Implement. / Plan review must explicitly accept both files.
4. Bis einschliesslich des letzten Gate-Laufs vor dem ersten C-05-Edit Review/Receipt, `global-ready` und beide Run-State-Oberflaechen frisch ausfuehren; diese bestandene Grenze ist historische Eingangsevidence und wird nach dem Delta nicht als erneut zu bestehende Freshness-Pruefung behauptet. / Run generic review, receipt, global-ready, and state gates through the last pre-edit boundary only.
5. Nach dem Delta in T052 den Python-Core, die beiden gleichwertigen CLI-Peers, Unix-Man-Page und PowerShell-Help/Cmdlet als eine Same-commit-Einheit implementieren. In T053 die isolierten Positiv-/Negativtests, temporaeren Projektionen, Security-/No-write-Pruefung, Help, Get-Help, Cmdlet, Man-Page, Paritaetscheckliste und vollstaendige lokale Testsuite abschliessen und beide Varianten manuell auf dem verfuegbaren macOS-Host ausfuehren; keine Windows-Evidence behaupten. Der Validator leitet die Stage ausschliesslich aus dem gebundenen aktiven Run-State ab und erlaubt fuer exakt diesen Run, Branch und Lifecycle nur `Plan`, `Implement`, `Validate`, `Publish`, `Review`, `MergeAndSync` oder `Retrospective`; `Plan` deckt den runner-owned post-Delta Analyze-Retry ab und ist keine allgemeine Stage-Ausnahme. Im selben Task den minimalen erforderlichen Workflow-Pfad `.github/workflows/powershell-analysis.yml` fuer die Feature-002-Snapshot-Suite in der bestehenden Linux-/macOS-/Windows-Matrix vorbereiten; seine reale Windows-Evidence wird erst am exakten reviewten Head akzeptiert. / T053 verifies both variants locally on macOS and prepares the minimal existing-matrix workflow update; the exact bound contract also admits Plan for runner-owned post-delta analysis retries, while Windows evidence is accepted only for the exact reviewed head.

### Phase B: Repraesentativer `C-05`-Slice / Representative `C-05` slice

1. Aktuelle `C-05`-Zeile und fehlschlagenden Status-Trennungscheck als Red-Evidence in `checklists/implementation-validation.md` erfassen. / Record the current row and failing status-separation check as red evidence.
2. Nur die Decision-Zelle `C-05` aendern; `Answered: IAD604` und `Open: DEC-T06` jeweils DE-first/EN-second sichtbar trennen. / Change only the `C-05` decision cell and separate the two states bilingually.
3. Gruenen `rg`-Nachweis, fokussierten Diff, positive Portfolio-Checks und unabhaengiges Semantikreview ausfuehren. / Run green pattern, focused diff, positive checks, and semantic review.
4. Bei anderem Delta oder Widerspruch sofort stoppen und neu planen. / Stop and replan on any other delta or contradiction.

### Phase C: Rollout `C-06` bis `C-09` / Rollout `C-06` through `C-09`

1. Statusprojektion exakt aus [data-model.md](data-model.md) uebernehmen. / Apply the exact status projection.
2. Nur die vier Decision-Zellen aendern; alle anderen Tabellenfelder bewahren. / Change only the four decision cells.
3. Decision Map und JSON-Vertrag read-only erneut abgleichen; keine Aenderung ohne neuen Driftbefund. / Reconcile validation-only sources read-only.
4. Alle sechs Portfolio-Checks ausfuehren und exakte erwartete Ergebnisse binden. / Run all six checks and bind exact results.

### Phase D: Nutzer-, Security-, Dokumentations-, AEPS- und Statistik-Evidence / User, security, documentation, AEPS, and statistics evidence

1. `first-reader-review-evidence.md`: getrennte unabhaengige Person ohne Spec-Kit-Vorerfahrung, `6/6`, null Blocker. / Independent first-reader evidence.
2. `accessibility-review-evidence.md`: DE/EN, B2, First-use, Heading, Links, lineare Reihenfolge, Textalternativen, Status und WCAG 2.2 AA; null Blocker. / Independent accessibility/language evidence.
3. Secret-Musterchecks ausfuehren; nach Candidate-Freeze `security-privacy-review-evidence.md` per path abschliessen. / Run scans and complete candidate-bound public-content review.
4. `documentation-impact-evidence.md` mit genau einer `UpdateRequired`-Entscheidung gegen den tatsaechlichen Kandidaten abschliessen. / Complete exactly one candidate-bound decision.
5. Nach abgeschlossener Implementierungsphase AEPS-Assessment und `docs/aeps/receipts/2026-08-30-meta-lh-02-portfolio-ownership.md`; Ledger-Derivationen nur bei echtem Finding als eine serielle Transaktion. / After completed implementation, perform the AEPS assessment; update ledger derivations only for a real finding.
6. T055 bis T058 bleiben als wahrheitsgemaess abgeschlossene historische Projektionssequenz erhalten. Ihr Ledger band die damalige Wegwerfprojektion; der spaetere reale Kandidatencommit machte deshalb beide realen Peers erwartbar `DRIFT`. Die Reparatur aendert weder Renderer noch Methodik und deutet diese Evidence nicht um. / T055 through T058 remain truthful historical projection evidence; the later real candidate commit necessarily made the real peers drift, without invalidating or reinterpreting the historical proof.

### Phase E: Historischer exakter Kandidat und lokale Konvergenz / Historical exact candidate and local convergence

Diese Sequenz dokumentiert den bereits verbrauchten 35-Pfad-Fixpunkt bis zum publizierten fehlgeschlagenen Head und erteilt keine erneute Amend-, Commit- oder Delivery-Autoritaet. Fuer jede weitere Arbeit gilt ausschliesslich Phase E2. / *This sequence records the consumed 35-path fixed point through the published failed head and grants no renewed authority; all future work follows Phase E2.*

1. `normal-feature-candidate` aus Allowlist aufloesen; jede Bedingung pruefen und eine exakte temporaere Pfadliste einfrieren. / Resolve the transaction and freeze an exact temporary path list.
2. Beide Delivery-Set-Oberflaechen mit jedem tatsaechlichen untracked Pfad ausfuehren; fremde Pfade ausschliessen und bewahren. / Run both delivery-set surfaces with every actual intended untracked path.
3. Alle lokalen Applicable Gates frisch ausfuehren; historische T010/T011-Evidence zeitlich korrekt erhalten, den post-Delta Snapshot-Vertrag ueber beide neuen Peers ausfuehren und N/A-Zeilen mit unveraenderter Begruendung/Trigger reviewen. / Rerun current gates while preserving the temporal distinction between historical generic evidence and the paired post-delta contract.
4. Der unpublizierte Kandidat `7b99227045de` und der einmal reviewte Amend bleiben historische Evidence. Die damalige Transaktion bewahrte Subject, Trailer und exakt 35 Pfade; ihre Autoritaet ist verbraucht. / The provisional candidate and its single reviewed amend remain historical evidence with consumed authority.
5. Der damalige unveraenderte Renderer erzeugte genau einen Methodik-v2-ausgeschlossenen Ledger-only-Synchronisationscommit. Diese abgeschlossene Sequenz darf fuer keinen neuen Head wiederverwendet werden. / The historical renderer sequence created its one methodology-excluded ledger-only synchronization commit and cannot authorize a new head.
6. Der daraus publizierte Statistik-Head `a78a78558459e32ad640c238f5eaf96337a70f83` ist der fehlgeschlagene PR-#29-Head und bleibt ausschliesslich historische Evidence. / The resulting published statistics head is the failed PR #29 head and remains historical evidence only.

### Phase E2: Begrenzte T079-CI-Reparatur / Bounded T079 CI remediation

1. Head `a78a78558459e32ad640c238f5eaf96337a70f83`, PR #29, 18 terminale Checks und die sechs technischen Fehler als unveraenderliche historische Evidence binden. Die verbrauchte Amend-/Statistikcommit-Autoritaet wird nicht wiederverwendet. / Preserve the failed published head and consumed transaction authority as history.
2. Nur die bereits akzeptierten Feature-002-Vertrags-, Test-, Workflow-, Help-, Man-, Quickstart-, Plan-/Task-/Gate-/Evidence-Pfade aendern. `docs/scripts/embedded-scripts.md` ist der einzige neue konditionale Pfad und wird nur nach beiden Previews mit `render-script-reference.*` deterministisch erzeugt; `docs/scripts/reference.md` und alle anderen generierten Dateien muessen unveraendert bleiben. / Change only accepted Feature-002 paths and conditionally regenerate only the embedded inventory after both previews.
3. In `powershell-analysis.yml` fuer `pull_request` exakt `github.event.pull_request.head.sha`, fuer `push` `github.sha` auschecken. Der Core behaelt lokale Git-Branchvalidierung und darf in CI `GITHUB_HEAD_REF` nur nach fail-closed Beweis von Event, Repository, akzeptierter Branch, Event-Head-SHA und ausgechecktem `HEAD` verwenden. / Bind explicit event heads and admit logical CI branch identity only through complete proof.
4. Die META-LH-02-Zielidentitaet bleibt normalisiert; rohe Receipt-/Review-Hashes bleiben unveraendert. Worktree-Rohbytes sind plattformabhaengig und kein Unix-Rohhash-Gate. Erforderliche Roh-Git-Evidence stammt aus den exakten Blobbytes am ausgecheckten Head. / Preserve normalized target identity and immutable raw receipt/review evidence, using exact Git blob bytes when raw Git proof is required.
5. Auf Windows Git-for-Windows-`bash.exe` explizit validieren und mit sicherem Argumentarray starten. WSL ohne installierte Distribution ist `Unavailable`; die erforderliche Bash-/PowerShell-Paritaetsevidence scheitert dann sichtbar und wird nie still uebersprungen. / Require executable Git-for-Windows Bash or fail closed.
6. Fokussierte Tests belegen synthetische Merge-/detached-Ablehnung, gueltige PR-Head-Aufloesung, LF-/CRLF-Aequivalenz mit substantiver Driftablehnung und Git-Bash-/WSL-Capability. Secure shell-free subprocess handling, Public-Klassifikation und No-secret-Grenze bleiben unveraendert. / Add only the focused fail-closed tests and retain secure process/public boundaries.
7. Danach T059 bis T073 vollstaendig neu ausfuehren: konditional 36 statt historisch 35 normale Kandidatenpfade, exakte Stage, alle lokalen Gates, neuer normaler Head, Methodik-v2-Regeneration und erforderlichenfalls ein neuer Ledger-only-Synchronisationshead. PR #29 Head/Body werden nur an den neuen finalen Head gebunden. / Restart the full fixed point and bind the PR only to the new final head.
8. Alle 18 Providerchecks und exakte Ubuntu-/macOS-/`windows-2022`-Workflow-/Job-/Runner-/Head-/Log-/Command-/Exit-Evidence sowie Review-Konvergenz muessen neu bestehen. Kein Admin-Bypass, Merge, Rename oder Closeout vor diesem Pass. / Rerun all provider and review gates; no bypass or downstream delivery before success.

### Phase F: Normaler Feature-Head / Normal feature head

1. Fuer den nach T079 neu erzeugten unveraenderlichen finalen Statistik-PR-Head alle 18 gemeldeten Checks, Required-Teilmenge, Workflow-/Log-Commands, `headRefOid`, Reviewentscheidung und Threads abgleichen. `PowerShell Static Analysis / PSScriptAnalyzer` muss fuer Ubuntu, macOS und `windows-2022` denselben explizit ausgecheckten Head binden und die Feature-002-Snapshot-Suite samt PowerShell-Peer ausfuehren; fuer jede Plattform sind Job, Runner, Log-URL, Command und erfolgreicher Exit erforderlich. Fehlende oder fehlschlagende technische Evidence blockiert ohne Admin-Bypass. / Reconcile all eighteen checks on the new explicit exact head and require successful Ubuntu, macOS, and Windows command evidence without bypass.
2. Einen vorlaeufigen Execution Record fuer bereits ausgefuehrte Gates und die Lifecycle-Voraussetzungen erstellen, ohne `PO-G32`, finales PreMerge oder PostMerge als bestanden zu behaupten. / Create a truthful preliminary execution record without pre-claiming the terminal gate or final snapshots.
3. Normaler Merge nur mit aktueller Authority und unabhaengiger Approval-Evidence. / Normal merge requires current authority and independent approval.
4. `PO-N01` bleibt `N/A`, bis genau sein letzter-Gate-Trigger vollstaendig erfuellt und neu reviewt ist; Bypass ersetzt nie Evidence, Review, Head oder Authority. / Narrow bypass remains unavailable until its complete trigger is re-reviewed.
5. Den unveraenderten finalen Statistik-Feature-Head mergen und `main` per Fast-forward synchronisieren; damit endet diese zweistufige normale Feature-Transaktion. / Merge the immutable final statistics feature head and fast-forward synchronize main; this ends the two-commit normal feature transaction.

### Phase G: Polish, terminaler Rename und kausaler Closeout / Polish, terminal rename, and causal closeout

1. Von synchronisiertem `main` einen dedizierten Rename-Branch erzeugen und Lifecycle-Datensatz, Original-/Archiv-Exklusivitaet, Intake-Normalhash, unveraenderte Receipt-/Review-Hashes, Series-Bindung, sauberen Worktree und aktuelle Authority pruefen. / Create a dedicated rename branch from synchronized main and validate every lifecycle binding.
2. Als letzte Aufgabe der Polish-Phase genau `scripts/rename-lastenheft.sh` oder den PowerShell-Peer ausfuehren; das Skript erzeugt den einzigen byteidentischen `R100`-Commit. / As the final Polish task, run exactly one existing rename-script surface to create the sole byte-identical `R100` commit.
3. Exakten Rename-Commit, beide Review-Oberflaechen, Series-Validator, Feature-002-Snapshot-Vertrag, null weitere Pfade und null Stage-Reste pruefen; generische Receipt-Source-Freshness und der generische `global-ready`-Befehl werden nach dem beabsichtigten Delta nicht verlangt. / Validate the exact rename with review, series, and feature-local snapshot evidence; do not require impossible generic post-delta freshness.
4. Fuer den unveraenderten Rename-Head eigene temporaere PreMerge-Evidence, Checks, Required-Teilmenge, Reviewentscheidung und Threads konvergieren lassen; danach genau diesen Head mergen und `main` erneut per Fast-forward synchronisieren. / Converge and merge the separate immutable rename head.
5. Erst jetzt kausales Schema-`2.0`-`PostMerge` fuer den akzeptierten Rename-PreMerge-Hash, den reviewten Rename-Head und den tatsaechlichen Rename-Merge-Commit mit leeren `changedPaths` erzeugen. / Only now create causal PostMerge for the merged rename transaction.
6. Nur bei notwendiger repository-lokaler Provider-Evidence und echtem Delta die exakte Drei-Pfad-Transaktion `tasks.md`, `autonomous-run-state.json`, `causal-closeout-evidence.json` verwenden; sonst keine leere Closeout-PR. Der Closeout darf seine eigene zukuenftige PR oder seinen Merge nicht behaupten. / Use the exact three-path closeout only for a real eligible delta and never self-claim its own publication.
7. Nach der materiellen Retrospektive T092 und vor terminaler Completion unter `docs/aeps/README.md` abschliessend Finding oder begruendet NoChange als getrennte Allowlist-Transaktion `final-aeps-reassessment` neu bewerten, unabhaengig validieren und einen echten Receipt-/Finding-Delta vor dem kausalen Closeout stabil liefern und `main` synchronisieren. Diese Transaktion verbreitert den exakten Drei-Pfad-Closeout nicht und erteilt weder Level-0-Handoff, Preset-Promotion, Upstream-Posting noch neue Ausfuehrungsautoritaet. / After T092 and before terminal completion, perform, independently validate, and stably deliver the separate final repository-contract AEPS transaction without broadening the exact three-path closeout or granting upstream authority.
8. Unabhaengig davon, ob der kausale Closeout repository-lokal persistiert wird oder wahrheitsgemaess keine Persistenz benoetigt, endet T093 mit einer ausdruecklichen No-next-run-Disposition. Der Run stoppt nach META-LH-02-Completion; META-LH-03 und jeder andere Spec-Kit-Lauf bleiben ungestartet und benoetigen einen neuen ausdruecklichen Auftrag. / Both the persisted and no-persistence paths end with an explicit no-next-run disposition; stop after META-LH-02 and do not start META-LH-03 or any other Spec Kit run.

## Gate- und Evidence-Inventar / Gate and Evidence Inventory

Die maschinenpruefbare Vollmenge steht in [autonomous-run-gate-requirements.json](autonomous-run-gate-requirements.json): `PO-G01` bis `PO-G32` sowie `PO-N04-script-tooling-parity` sind anwendbar; `PO-N01`, `PO-N02`, `PO-N03` und `PO-N05` bis `PO-N08` sind mit Begruendung und Neubewertungstrigger `N/A`. Die Datei nennt fuer jedes anwendbare Gate exakten Scope, Command-Token, Runner-/Plattformtoken, Owner, Reviewer und Evidence-Pfad. Diese Zusammenfassung ersetzt die Datei nicht. / *The machine-readable full set contains thirty-three applicable gates and seven justified non-applicable gates.*

| Gruppe / Group | Gates | Primaere Evidence / Primary evidence |
|---|---|---|
| Input und Lifecycle / input and lifecycle | `PO-G01`–`PO-G07` | historische generische Pre-Delta-Eingangsevidence plus aktueller Feature-002-Snapshot-, Review- und Run-State-Nachweis / historical generic entry evidence plus current local snapshot, review, and run-state proof |
| Slice und Rollout / slice and rollout | `PO-G08`–`PO-G09` | `checklists/implementation-validation.md` |
| Sechs Portfolio-Checks / six portfolio checks | `PO-G10`–`PO-G15` | `checklists/implementation-validation.md` |
| First-reader und A11Y/B2 | `PO-G16`–`PO-G17` | `first-reader-review-evidence.md`, `accessibility-review-evidence.md` |
| Security/Privacy und Script-Paritaet / Security/privacy and script parity | `PO-G18`–`PO-G21`, `PO-N04` | Implementierungscheckliste, `security-privacy-review-evidence.md`, `checklists/snapshot-tooling-parity.md`, lokale macOS-Man-/Help-/Varianten-Evidence plus exakte Linux-/macOS-/Windows-CI-Head-Evidence / local variant evidence plus exact-head cross-platform CI evidence |
| Documentation, AEPS, Statistik | `PO-G22`–`PO-G24` | `documentation-impact-evidence.md`, AEPS-Receipt, `docs/project-statistics.md` |
| Delivery Set und Stage | `PO-G25`–`PO-G27` | Implementierungscheckliste plus temporaere eingefrorene Pfadliste / frozen temporary path list |
| CI, Review und PreMerge | `PO-G28`–`PO-G31` | temporaerer Execution Record, Provider-URLs und PreMerge-JSON ausserhalb des Repositorys / temporary execution record, provider URLs, and PreMerge JSON |
| Terminaler Lifecycle / terminal lifecycle | `PO-G32` | `intake-lifecycle.json`, Implementierungscheckliste und temporaere Exact-head-/PreMerge-/PostMerge-Evidence / lifecycle record plus exact rename evidence |
| Bedingte oder ausgeschlossene Gates / conditional or excluded | `PO-N01`–`PO-N03`, `PO-N05`–`PO-N08` | `checklists/implementation-validation.md`, spaeter gegebenenfalls `causal-closeout-evidence.json` / later causal evidence if triggered |

## Shared Writer und Serialisierung / Shared Writers and Serialization

| Shared Writer | Einziger Schreibzeitpunkt / Sole write point | Folgende Abhaengigkeit / Downstream dependency |
|---|---|---|
| `requirements/baseline/portfolio-ownership.md` | Phase B/C durch Portfolio Owner, Slice vor Rollout. / Phases B/C by Portfolio Owner, slice before rollout. | Alle Reviews, sechs Checks und Candidate-Freeze konsumieren den finalen Inhalt. / Reviews, checks, and freeze consume final content. |
| `specs/002-portfolio-ownership/checklists/implementation-validation.md` | Fortlaufend durch Autonomous Run Operator, aber an jeder Gate-Grenze atomar. / Incremental but atomic at each gate boundary. | PreMerge und Task-/State-Abschluss. / PreMerge and completion. |
| `docs/aeps/*` | Serielle Implementierungsbewertung in T054; abschliessende Finding-oder-NoChange-Neubewertung nach der materiellen Retrospektive T092 und vor T093. / Serial implementation assessment, then the final reassessment after T092 and before T093. | Jeweils unabhaengige Validierung; kein Level-0-, Preset-, Upstream- oder Folgelauf-Schritt. / Independent validation without upstream or next-run authority. |
| `docs/project-statistics.md` | T055 bis T058, `7b992270`, das verbrauchte Amend und die Ledger-only-Synchronisation bis `a78a785` bleiben historisch. Nach dem neuen T079-Fixpunkt rendert derselbe unveraenderte Renderer vom sauberen neuen 36-Pfad-Reparatur-Head; nur bei weiterem Drift folgt hoechstens ein neuer `statistics-head-sync`-Commit nur fuer das Ledger. / Historical projection and consumed sequence remain evidence; the new T079 repair head is authoritative and permits at most one new ledger-only sync only if drift remains. | Beide realen Peers `CURRENT`/`0` auf dem neuen finalen Statistik-Head; danach bindet jede betroffene Evidence denselben Head. / Both real peers CURRENT on the new final statistics head and every affected gate rebound to it. |
| `tasks.md` und `autonomous-run-state.json` | Nur zustaendige nachgelagerte Phase; keine parallelen Writes. / Downstream owner only. | Task-Hash, Closeout und PostMerge. |
| `intake-lifecycle.json` | Snapshot in dieser Remediation, danach im normalen Feature-Kandidaten vor Freeze unveraendert. / Snapshot bound in this remediation, then immutable in the normal candidate. | Feature-002-Snapshot-, Review-/Series-Aufloesung und terminaler Rename. / Local snapshot, review/series resolution, and terminal rename. |
| META-LH-02 Original-/Archivpfad | Ausschliesslich durch den vorhandenen Rename-Skript-Commit nach normalem Merge; letzte Aufgabe der Polish-Phase. / Only by the existing script after normal merge; final Polish task. | Eigener unveraenderlicher Rename-Head, danach kausales PostMerge. / Separate immutable rename head, then causal PostMerge. |
| `causal-closeout-evidence.json` | Nur kausal nach Merge/Sync und nur bei echtem Delta. / Only causally after merge/sync and real delta. | Exakte Drei-Pfad-Closeout-Pruefung. |

## CI-Mapping / CI Mapping

| Workflow | Job / Check | Runner | Lokale oder feature-spezifische Ergaenzung / Local or feature-specific complement |
|---|---|---|---|
| `homogeneity-check.yml` | `Homogeneity Check / homogeneity` | `ubuntu-latest` | T079 muss den konditionalen Generated-Update-Pfad `docs/scripts/embedded-scripts.md` ueber beide Previews, deterministischen Renderer, exakten Diff und beide Check-only-Peers schliessen; `docs/scripts/reference.md` bleibt unveraendert. Semantik-, B2- und A11Y-Review bleiben getrennt. / T079 closes only the generated embedded-inventory drift through the canonical renderer and both peer checks. |
| `public-readiness.yml` | `Public Readiness / Repository baseline` | `ubuntu-latest` | `gitleaks`, beide Scanner und per-path Public-Content-Review. / Machine scans plus per-path review. |
| `public-readiness.yml` | `Public Readiness / .NET contract` | Ubuntu/Windows/macOS | Erwartet `NotApplicable` ohne Produkt-Scaffold; kein Feature-Gate-Ersatz. / Expected N/A absent product scaffold. |
| `powershell-analysis.yml` | `PowerShell Static Analysis / PSScriptAnalyzer` | Ubuntu 22.04/macOS 14/Windows 2022 | `pull_request` checkt explizit `github.event.pull_request.head.sha`, `push` `github.sha` aus. Der fail-closed Resolver beweist die logische Branchidentitaet gegen Event, Repository und tatsaechliches `HEAD`; synthetische Merge-/detached-Artefakte bleiben unzulaessig. Die Feature-002-Suite und beide Peer-Grenzen laufen auf allen drei Runnern; Windows nutzt validiertes Git-for-Windows-Bash statt unbrauchbarem WSL-Launcher. / Explicit event-head checkout, proven logical branch identity, and executable Git-for-Windows Bash are mandatory on the three-runner matrix. |
| `maintenance-tui.yml` | `Maintenance TUI` | Ubuntu 22.04/macOS 14/Windows 2022 | Unveraenderte Regression; sechs Portfolio-Laeufe bleiben separat. / Unchanged regression; portfolio runs remain separate. |

Vor Merge gilt nicht nur die statische Tabelle: `gh pr checks` und Workflow-/Run-Logs bestimmen die tatsaechliche vollstaendige Checkmenge fuer den exakten Head. Jeder zusaetzlich gemeldete nicht erfolgreiche Check blockiert ebenfalls. / *Before merge the actual full reported check inventory for the exact head governs; every additional non-successful reported check also blocks.*

## Projektstruktur / Project Structure

### Feature-Dokumentation / Feature documentation

```text
specs/002-portfolio-ownership/
├── spec.md
├── clarification-report.md
├── checklists/
│   ├── requirements.md
│   └── implementation-validation.md                 # spaetere Phase / later phase
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   ├── planning-validation-contract.md
│   ├── delivery-allowlist.json
│   ├── validate_meta_lh02_snapshot.py               # spaetere Implementierung / later implementation
│   ├── validate-meta-lh02-snapshot.sh               # Bash-Peer, spaetere Implementierung / later implementation
│   ├── validate-meta-lh02-snapshot.ps1              # PowerShell-Peer + Cmdlet / PowerShell peer plus cmdlet
│   ├── test_validate_meta_lh02_snapshot.py          # spaetere Implementierung / later implementation
│   └── fixtures/                                     # isolierte Fail-closed-Faelle / isolated fail-closed cases
├── checklists/snapshot-tooling-parity.md                  # spaetere Paritaetsevidence / later parity evidence
├── autonomous-run-gate-requirements.json
├── intake-lifecycle.json                            # normaler Kandidat, danach unveraendert / normal candidate, then immutable
├── autonomous-run-state.json
├── autonomous-run-evidence.md
├── tasks.md                                          # nur /speckit-tasks / only /speckit-tasks
├── first-reader-review-evidence.md                   # spaetere Phase / later phase
├── accessibility-review-evidence.md                  # spaetere Phase / later phase
├── security-privacy-review-evidence.md               # spaetere Phase / later phase
├── documentation-impact-evidence.md                  # spaetere Phase / later phase
└── causal-closeout-evidence.json                     # nur kausal nach Merge / causal only
```

### Fachliche und Shared-Writer-Artefakte / Domain and shared-writer artefacts

```text
requirements/baseline/
├── portfolio-ownership.md                            # einziger fachlicher Delta / sole domain delta
└── portfolio-ownership.json                          # validation-only

docs/decisions/open-decisions.md                      # validation-only
specs/intake-review-fixtures/meta-lh-02/              # validation-only
docs/man/validate-meta-lh02-snapshot.1                 # spaetere Unix-Man-Page / later Unix man page
.github/workflows/powershell-analysis.yml              # minimaler Feature-002-Matrixdelta / minimal Feature-002 matrix delta
docs/scripts/embedded-scripts.md                       # konditional generiert in T079 / conditionally generated in T079

docs/aeps/
├── findings-ledger.md                                # nur bei Finding / only for a finding
├── finding-to-preset-candidate-matrix.md             # nur bei Finding / only for a finding
├── preset-gap-analysis.md                            # nur bei Finding / only for a finding
├── upstream-handoff.md                               # nur lokaler Status / local status only
└── receipts/2026-08-30-meta-lh-02-portfolio-ownership.md

docs/project-statistics.config.json                   # kanonische Konfiguration, unveraendert / canonical config
docs/project-statistics.md                            # generiert nach Implement / generated after implementation
```

**Strukturentscheidung / Structure decision**: Es gibt kein `src/`, Produkt-, API-, UI- oder Runtime-Projekt. Der einzige ausfuehrbare Zusatz ist das read-only Feature-002-Vertragswerkzeug aus Python-Core, Bash-/PowerShell-Peer, Man-/Help-/Cmdlet-Oberflaeche, Tests, isolierten Fixtures/temporaeren Projektionen und Paritaetsevidence; externe API-Contracts bleiben `N/A`. / *There is no product/API/UI/runtime project; the sole executable addition is the isolated, paired, read-only Feature-002 validation tool.*

## Komplexitaetsverfolgung / Complexity Tracking

Keine Verfassungsverletzung benoetigt eine Ausnahme. Die zusaetzlichen Planungsartefakte sind erforderlich, weil der autonome Schema-2.0-Evidence-Vertrag und die exakte Liefermengenpruefung maschinenlesbare Inputs verlangen; sie erweitern den fachlichen Scope nicht. / *No constitutional violation requires an exception. The supporting planning artefacts are required by the autonomous evidence and exact delivery-set contracts and do not expand domain scope.*
