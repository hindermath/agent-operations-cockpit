# Forschung: Portfolio-Ownership / Research: Portfolio Ownership

## Zweck und Methode / Purpose and method

Diese Phase klaert die technische Planung fuer den begrenzten dokumentarischen Delta in `requirements/baseline/portfolio-ownership.md`. Grundlage sind die geklaerte Spezifikation, die formale `44/44`-Checkliste, die aktuellen Repository-Validatoren, die Dokumentations-, Statistik- und AEPS-Vertraege sowie der aktive autonome Lauf. Alle Befunde wurden repository-lokal und read-only erhoben. / *This phase resolves technical planning for the bounded documentary delta in `requirements/baseline/portfolio-ownership.md`. It uses the clarified specification, formal `44/44` checklist, current repository validators, documentation/statistics/AEPS contracts, and the active autonomous run. All findings were gathered locally and read-only.*

## Entscheidungen / Decisions

### R-001: Minimaler fachlicher Delta / Minimal domain delta

- **Entscheidung / Decision**: Nur die Decision-Zellen der Zeilen `C-05` bis `C-09` in `requirements/baseline/portfolio-ownership.md` werden fachlich geaendert. `requirements/baseline/portfolio-ownership.json`, `docs/decisions/open-decisions.md` und `specs/intake-review-fixtures/meta-lh-02/validate-portfolio.*` bleiben validation-only, solange kein neuer reproduzierbarer Driftbefund vorliegt. / Only the decision cells in rows `C-05` through `C-09` receive domain edits. The JSON contract, Decision Map, and current validators remain validation-only absent a new reproducible drift finding.
- **Begruendung / Rationale**: Die aktuelle Decision Map weist drei offene, 23 beantwortete und drei supersedierte Decisions aus. Die Markdown-Uebersicht vermischt dagegen in den betroffenen Zellen geschlossene und offene Eintraege. / The current Decision Map records three open, 23 answered, and three superseded decisions, while the affected Markdown cells mix closed and open entries.
- **Verworfene Alternative / Alternative considered**: Maschinenvertrag, Decision Map oder Validator vorsorglich anzupassen waere unbelegter Scope-Zuwachs. / Proactively changing the machine contract, Decision Map, or validator would be unproven scope expansion.

### R-002: Repraesentativer Red/Green-Slice / Representative red/green slice

- **Entscheidung / Decision**: `C-05` ist der einzige erste vertikale Slice. Der rote Nachweis bindet die aktuelle vermischte Form. Der gruene Nachweis verlangt getrennte, textlich benannte Statusgruppen fuer beantwortetes `IAD604` und offenes `DEC-T06`. Erst danach werden `C-06` bis `C-09` nach derselben Regel bearbeitet. / `C-05` is the sole first vertical slice. Red evidence binds the current mixed representation; green evidence requires separate, textually named status groups for answered `IAD604` and open `DEC-T06`. Only then is the rule rolled out to `C-06` through `C-09`.
- **Begruendung / Rationale**: Dieser Slice prueft den schwierigsten Mischfall und verhindert einen breiten, unbewiesenen Tabellenedit. / This slice tests the hardest mixed-status case and prevents a broad unproven table edit.
- **Verworfene Alternative / Alternative considered**: Alle fuenf Zeilen in einem Schritt zu aendern wuerde den geforderten fehlschlagenden Ausgangsnachweis verdecken. / Editing all five rows at once would obscure the required failing baseline.

### R-003: Isolierter historischer Programmevidence-Vertrag / Isolated historical programme-evidence contract

- **Entscheidung / Decision**: Der fachliche Portfolio-JSON-Vertrag und alle installierten/shared Validatoren bleiben unveraendert. Feature 002 plant stattdessen `contracts/validate_meta_lh02_snapshot.py` als dependency-freien Python-3-Standardbibliotheks-Core, die gleichwertigen Peers `contracts/validate-meta-lh02-snapshot.sh` und `contracts/validate-meta-lh02-snapshot.ps1`, die Unix-Man-Page `docs/man/validate-meta-lh02-snapshot.1`, die Paritaetscheckliste `checklists/snapshot-tooling-parity.md`, isolierte Fixtures und `contracts/test_validate_meta_lh02_snapshot.py`. Der Core darf Logik aus Feature 001 adaptieren, bleibt aber vollstaendig feature-lokal. / Keep the domain contract and every installed/shared validator unchanged; add only a Feature-002-local standard-library core, constitution-compliant paired surfaces, Unix manual, parity checklist, and isolated fixtures/tests, adapting Feature 001 where useful.
- **Begruendung / Rationale**: Der akzeptierte Receipt ist korrekt unveraenderlich, bindet aber den Vor-Implementierungs-Hash von `portfolio-ownership.md`. Das verlangte C-05-bis-C-09-Delta macht nur generische Source-Freshness erwartbar rot; ein run-, branch-, lifecycle- und stage-gebundener Snapshot bewahrt historische Programmeannahme, ohne die Receipt-Bytes umzuschreiben oder eine allgemeine Ausnahme zu schaffen. / The immutable receipt correctly binds the pre-implementation source. A narrowly qualified snapshot preserves acceptance history without rewriting evidence or creating a general exception.
- **Verworfene Alternative / Alternative considered**: Intake, Receipt oder Review neu schreiben; C-05-bis-C-09 zuruecksetzen; Feature 001 oder installierte Validatoren aendern; generische Freshness nach dem Delta weiterhin als Pass verlangen. Alle Optionen verletzen Scope, Immutabilitaet oder den akzeptierten fachlichen Delta. / Rewriting accepted evidence, reverting the domain delta, changing Feature 001/shared validators, or continuing to require impossible generic freshness all violate the accepted boundary.

### R-004: Gate-Requirements Schema 1.0, Gate-Evidence Schema 2.0

- **Entscheidung / Decision**: `autonomous-run-gate-requirements.json` wird aus der installierten Requirements-Vorlage mit Schema `1.0` erzeugt. Die installierte Schema-`2.0`-Vorlage wird ausschliesslich fuer temporaere `PreMerge`- und kausale `PostMerge`-Evidence verwendet. / The gate-requirements file is created from the installed schema-`1.0` requirements template. The installed schema-`2.0` template is used only for temporary `PreMerge` and causal `PostMerge` evidence.
- **Begruendung / Rationale**: `autonomous-evidence-core.py` verlangt fuer Requirements exakt Schema `1.0`; die Man-Page des Gate-Evidence-Validators nennt providerneutrale Schema-`2.0`-Snapshots. Eine Schema-`2.0`-Requirements-Vorlage ist nicht installiert. / The installed validator requires schema `1.0` for requirements, while its manual defines schema-`2.0` evidence snapshots. No schema-`2.0` requirements template is installed.
- **Verworfene Alternative / Alternative considered**: Ein nicht unterstuetztes Requirements-Schema zu erfinden oder Preset-/Validatorcode zu aendern waere falsch und ausserhalb des Scopes. / Inventing an unsupported requirements schema or changing preset/validator code would be incorrect and out of scope.

### R-005: Vor-Implementierungspruefung der Gate-Datei / Pre-implementation check of the gate file

- **Entscheidung / Decision**: Vor dem ersten Domain-Edit prueft die installierte Python-Core-Funktion `validate_requirements()` die Requirements-Datei schema-only; eine unabhaengige Plan-Review-Rolle prueft Vollstaendigkeit, Commands, Runner-/Plattformtoken, Owner, Evidence-Pfade und N/A-Trigger. Die vollstaendige installierte Bash-/PowerShell-Gate-Evidence-Pruefung folgt fuer den exakten reviewten Head, sobald echte Evidence vorliegt. / Before the first domain edit, the installed Python core function `validate_requirements()` performs the schema-only check and an independent plan reviewer checks completeness. The full Bash/PowerShell gate-evidence validation follows for the exact reviewed head when real evidence exists.
- **Begruendung / Rationale**: Der oeffentliche Gate-Evidence-Wrapper verlangt bereits vollstaendige echte `Pass`-Evidence fuer jedes anwendbare Gate. Eine vorgezogene Erfolgsevidence fuer noch nicht ausgefuehrte Implementierungs- oder Provider-Gates waere unzulaessig. / The public gate-evidence wrapper already requires real passing evidence for every applicable gate. Pre-claiming future implementation or provider gates would be invalid.
- **Verworfene Alternative / Alternative considered**: Zukunftsevidence als `Pass` zu erfinden oder anwendbare Gates voruebergehend `N/A` zu setzen. / Fabricating future passing evidence or temporarily marking applicable gates `N/A`.

### R-006: Explizite Liefermengen statt Verzeichnis-Stage / Explicit delivery sets instead of directory staging

- **Entscheidung / Decision**: `contracts/delivery-allowlist.json` trennt Planungs-, normale Feature- und kausale Closeout-Transaktionen. Vor jedem spaeter autorisierten Commit wird aus der passenden Transaktion eine exakte aktuelle Pfadliste eingefroren und mit beiden `validate-autonomous-delivery-set`-Oberflaechen, `git status --porcelain=v1 --untracked-files=all`, der exakten Stage und `git diff --cached --check` abgeglichen. / The delivery allowlist separates planning, normal feature, and causal closeout transactions. Before every later authorised commit, the matching exact current path list is frozen and reconciled with both delivery-set surfaces, porcelain status, the exact stage, and `git diff --cached --check`.
- **Begruendung / Rationale**: Der Validator nimmt explizite untracked Pfade entgegen, meldet fremde untracked Dateien und veraendert Index und Worktree nicht. / The validator accepts explicit untracked paths, reports unrelated untracked files, and does not mutate index or worktree.
- **Verworfene Alternative / Alternative considered**: `git add .`, Verzeichnis-Wildcards oder eine einzige uebergrosse Transaktion. / `git add .`, directory wildcards, or one oversized transaction.

### R-007: Shared Writer werden serialisiert / Shared writers are serialized

- **Entscheidung / Decision**: Die Domain-Tabelle, AEPS-Dateien, Statistik-Ledger, Run-State und Tasks sind jeweils Single-writer. Reihenfolge: Domain-Slice und Rollout; Review-Evidence; AEPS-Assessment; Statistik-Renderer nach abgeschlossener Implementierung; Candidate-Freeze; spaeterer kausaler Closeout. / The domain table, AEPS files, statistics ledger, run state, and tasks are single-writer. Order: domain slice and rollout; review evidence; AEPS assessment; statistics render after implementation; candidate freeze; later causal closeout.
- **Begruendung / Rationale**: Diese Pfade sind gemeinsam genutzte kanonische oder generierte Artefakte. Parallele Writes wuerden Hash- und Evidence-Bindungen invalidieren. / These paths are shared canonical or generated artefacts. Parallel writes would invalidate hash and evidence bindings.
- **Verworfene Alternative / Alternative considered**: Parallele Agenten- oder RAW-Arbeit. / Parallel agent or RAW work.

### R-008: Documentation Impact, Statistik und AEPS / Documentation Impact, statistics, and AEPS

- **Entscheidung / Decision**: Genau eine Entscheidung bleibt `UpdateRequired`. Nach abgeschlossener Implementierungsphase wird gemaess `docs/aeps/README.md` zuerst ein Finding oder begruendetes No-change-Receipt unter `docs/aeps/receipts/2026-08-30-meta-lh-02-portfolio-ownership.md` erzeugt; das Ledger und seine Ableitungen aendern sich nur bei einem echten neuen Finding. Danach wird `docs/project-statistics.md` aus Methodik-v2-Quelle gegen den finalen Ledger-Zustand gerendert. / Exactly one decision remains `UpdateRequired`. After completed implementation, the AEPS assessment first records a finding or justified no-change receipt; ledger derivations change only for a genuine new finding. Statistics then render from the methodology-v2 source against the final ledger state.
- **Begruendung / Rationale**: Beide Vertraege definieren explizite Trigger und Source-of-Truth-Grenzen. / Both contracts define explicit triggers and source-of-truth boundaries.
- **Verworfene Alternative / Alternative considered**: Statistik in Plan vorwegzunehmen oder AEPS still zu ueberspringen. / Pre-rendering statistics during planning or silently skipping AEPS.

### R-009: CI ist zusaetzlich und traegt die reale Windows-Evidence / CI is additive and carries real Windows evidence

- **Entscheidung / Decision**: Der exakte PR-Head muss alle gemeldeten Checks bestehen. Die vorhandene `PowerShell Static Analysis / PSScriptAnalyzer`-Matrix laeuft auf Linux, macOS und Windows, fuehrt die neuen Feature-002-Snapshot-Tests jedoch derzeit nicht aus. Deshalb wird `.github/workflows/powershell-analysis.yml` als minimaler erforderlicher In-scope-Pfad des normalen Kandidaten geplant: Nach Python-Setup fuehrt derselbe Matrixjob die isolierte Testsuite und den PowerShell-Snapshot-Peer aus; auf `windows-2022` bindet er die reale Windows-Ausfuehrung des exakt reviewten Heads. Linux/macOS bleiben in derselben Matrix erforderlich. T077/T080/T084/T090 muessen Workflow, Job, Runner, Log-URL, Head-SHA und tatsaechlichen Command fail-closed abgleichen. Die sechs Portfolio-Laeufe, lokale manuelle Variantenpruefung und semantische Reviews bleiben zusaetzliche Evidence. / The exact PR head must pass every reported check. The existing PowerShell Static Analysis matrix spans Linux, macOS, and Windows but does not currently execute the Feature-002 snapshot suite. Plan the minimal in-scope update to `.github/workflows/powershell-analysis.yml`: after Python setup, the same matrix job runs the isolated suite and PowerShell snapshot peer, with `windows-2022` providing real Windows execution for the exact reviewed head and Linux/macOS remaining mandatory. T077/T080/T084/T090 bind workflow, job, runner, log URL, head SHA, and actual command fail closed. Local and semantic evidence remains additive.
- **Begruendung / Rationale**: Die Constitution verlangt manuelle Verifikation beider Varianten, nicht einen lokalen Host jedes Betriebssystems. Beide Varianten koennen wahrheitsgemaess auf macOS manuell geprueft werden; reale Windows-Assurance wird spaeter am unveraenderten reviewten Head staerker und reproduzierbar durch CI belegt. / The constitution requires manual verification of both variants, not a local host for every operating system. Both variants can be manually verified on macOS, while real Windows assurance is later proven reproducibly for the immutable reviewed head in CI.
- **Verworfene Alternative / Alternative considered**: Lokale Windows-Evidence vor dem ersten Commit zu verlangen, PowerShell 7 auf macOS als Windows umzudeuten, einen allgemeinen gruenden Checknamen ohne Job-/Log-Command als Beleg zu akzeptieren oder Admin-Bypass fuer fehlende Windows-Evidence zu verwenden. / Requiring local Windows before the first commit, relabelling macOS PowerShell as Windows, accepting a green check name without job/log command evidence, or bypassing missing Windows evidence are rejected.

### R-010: Enger Bypass und kausaler Closeout / Narrow bypass and causal closeout

- **Entscheidung / Decision**: Der Bypass bleibt in den PreMerge-Requirements `N/A` und darf erst nach seinem vollstaendigen letzten-Gate-Trigger neu bewertet werden. `PreMerge` bleibt temporaer. `PostMerge` entsteht erst nach dem tatsaechlichen Merge und bindet den akzeptierten PreMerge-Normalhash, den Merge-Commit und leere `changedPaths`. Repository-lokale Persistenz ist nur ueber den vorbenannten `causal-closeout-evidence.json`-Pfad und nur bei echtem zulaessigem Delta erlaubt. / Bypass remains `N/A` in pre-merge requirements until every last-gate precondition is met. `PreMerge` remains temporary; `PostMerge` is created only causally after merge. Repository-local persistence is permitted only through the pre-named closeout path and only for a real eligible delta.
- **Begruendung / Rationale**: Ein Snapshot kann seinen eigenen reviewed Head oder zukuenftigen Merge nicht wahrheitsgemaess vorwegnehmen. / A snapshot cannot truthfully pre-claim its own reviewed head or a future merge.
- **Verworfene Alternative / Alternative considered**: Bypass als standing authority oder eine leere Closeout-PR. / Standing bypass authority or an empty closeout PR.

### R-011: Archivbewusster terminaler Lastenheft-Rename / Archive-aware terminal intake rename

- **Entscheidung / Decision**: Der normale Feature-Kandidat enthaelt `specs/002-portfolio-ownership/intake-lifecycle.json` mit Schema `1.1`, exaktem `META-LH-02`-Record und 14-Ziele-`programmeEvidenceSnapshot`. Nach Merge und Fast-forward-Sync laeuft auf einem eigenen Branch genau `terminal-intake-rename`: Das vorhandene Bash- oder PowerShell-Skript erzeugt als letzte Polish-Aufgabe einen byteidentischen `R100`-Commit. Danach pruefen der Feature-002-Snapshot-Vertrag, beide Review-Oberflaechen und Series archivbewusst read-only; generische Receipt-Source-Freshness und generisches `global-ready` sind keine post-Delta-Erfolgsanforderung. Lifecycle, Receipt, Review und Series werden nicht umgeschrieben. / The normal candidate carries the exact lifecycle record and fourteen-target snapshot; after the separate R100 rename, only the feature-local snapshot contract, both review peers, and series validation are current gates.
- **Begruendung / Rationale**: Die installierten Authoring-, Review- und Sequencing-Validatoren suchen `specs/*/intake-lifecycle.json`, verlangen genau einen physischen Original- oder Archivpfad und pruefen den akzeptierten Normalhash. Diese vorhandene Aufloesung bewahrt die historischen Receipt-/Review-/Series-Pfade und -Hashes, ohne die aktive Eingabe inhaltlich zu veraendern. Ein eigener Rename-Head verhindert zugleich, dass der bereits reviewte normale Feature-Head oder ein PostMerge-Snapshot durch eine spaetere Path-Mutation selbst invalidiert wird. / The installed validators already resolve one unambiguous lifecycle record and preserve immutable historical bindings. A separate reviewed rename head avoids mutating either the normal reviewed feature head or a causal PostMerge claim.
- **Verworfene Alternative / Alternative considered**: Den Rename als `N/A` zu belassen, Receipt/Review/Series auf den Archivpfad umzuschreiben, den Rename ungereviewt direkt auf `main` auszufuehren oder ihn in einen PostMerge-Snapshot mit angeblich leeren `changedPaths` einzuschliessen. / Leaving the rename non-applicable, rewriting immutable lifecycle evidence, committing directly to main without review, or hiding the path mutation behind an empty-changes PostMerge claim.

### R-012: Zeitliche Gate-Teilung nach `GlobalReady` / Temporal gate split after `GlobalReady`

- **Entscheidung / Decision**: Die vor dem ersten Domain-Edit belegten generischen Receipt- und `global-ready`-Paesse bleiben historische Eingangsevidence. Fuer `Plan`, `Implement`, `Validate`, `Publish`, `Review`, `MergeAndSync` und `Retrospective` ist nur der Feature-002-Snapshot-Vertrag aktuell bindend. `Plan` gilt ausschliesslich fuer den runner-owned post-Delta Analyze-Retry im exakten aktiven Run-/Branch-/Lifecycle-Vertrag. Der Vertrag verifiziert die 14 akzeptierten Zielhashes gegen die historische Zielaufloesung, rohe Receipt-/Review-Bytes, eindeutige aktuelle Ready-Leaves, beide Review-Validatoren sowie den exakten Run, Branch und Lifecycle. / Preserve generic passes as historical entry evidence and use only the feature-local snapshot contract for the enumerated stages; Plan is limited to the exact bound runner-owned post-delta analysis retry.
- **Begruendung / Rationale**: Damit wird exakt ein nicht mehr erfuellbarer Freshness-Teil ersetzt, waehrend alle uebrigen Acceptance- und Driftkontrollen bestehen bleiben. / This replaces exactly one impossible freshness component while retaining every other acceptance and drift control.
- **Verworfene Alternative / Alternative considered**: Stage-unabhaengige Ausnahme, frei waehlbarer CLI-Stage, Abschalten der Review-Validatoren oder Zulassen mehrerer Ready-Leaves. / Stage-independent exceptions, caller-selected stages, disabling review validators, or accepting multiple Ready leaves.

### R-013: Verfassungskonforme kritische CLI-Paritaet / Constitution-compliant critical CLI parity

- **Entscheidung / Decision**: `PO-N04-script-tooling-parity` ist `Applicable`. T052 liefert Python-Core, Bash- und PowerShell-Core-7-Peer sowie Man-Page und vollstaendige bilinguale PowerShell Comment Help gemeinsam. Die PowerShell-Datei stellt das genehmigte Advanced Function/Cmdlet `Test-AocMetaLh02Snapshot` bereit. Bash nutzt `set -euo pipefail`, vollstaendige Quoting- und `--`-Disziplin; PowerShell nutzt `#Requires -Version 7`, `Set-StrictMode -Version Latest`, `$ErrorActionPreference = 'Stop'`, validierte Parameter und wird mit `pwsh -NoProfile` geprueft. Beide Oberflaechen besitzen gleichwertige Help-, Ausgabe- und Exitcode-Vertraege. Der Validator ist immer read-only; deshalb existiert kein separater mutierender Dry-run-Modus. T053 bindet `checklists/snapshot-tooling-parity.md`, die manuelle Ausfuehrung beider Varianten auf dem verfuegbaren macOS-Host und Same-commit-Pruefung. Echte Windows-Evidence wird nicht lokal behauptet, sondern bleibt exakter CI-Head-Mergeblocker. / PO-N04 is applicable. T053 proves both variants manually on the available macOS host and same-commit delivery without claiming Windows; real Windows execution remains an exact-head CI merge blocker.
- **Begruendung / Rationale**: Der Snapshot-Vertrag ist gate-kritisch und script-shaped. Das Ausfuehren vorhandener Review-Peers macht einen neuen unpaarigen Python-CLI-Einstieg nicht verfassungskonform. / The snapshot contract is gate-critical and script-shaped; invoking existing review peers does not make a new unpaired Python CLI constitution-compliant.
- **Verworfene Alternative / Alternative considered**: Den Python-CLI direkt als einzige Nutzeroberflaeche ausliefern, Paritaet indirekt aus installierten Review-Skripten ableiten oder die Constitution in diesem Feature aendern. / Shipping Python as the sole user surface, inferring parity indirectly, or changing the constitution in this feature.

### R-014: Fail-closed und sichere ausfuehrbare Evidence / Fail-closed and secure executable evidence

- **Entscheidung / Decision**: Der Testvertrag prueft positiv sowie fail-closed falschen Run, Branch und Stage, nicht exakte Lifecycle-Form, beide/keinen Original-/Archivpfad, akzeptierte Zielhash-Drift, Receipt-/Review-Byte-Drift, inaktiven State, doppelten Ready-Leaf und den Ausfall jeder einzelnen installierten Review-Oberflaeche. Die sechs bereits benannten getrackten Fixtures bleiben; weitere Faelle werden in einem temporaeren Verzeichnis als Projektionen erzeugt. Vor und nach jedem Test wird der Repository-Zustand read-only verglichen. Security-Review deckt NIST SSDF, CWE Top 25, Python-Grenzvalidierung, keine unsichere Deserialisierung oder dynamische Ausfuehrung, eingeschraenkte repository-relative Pfade, `subprocess.run` ohne Shell, Standardbibliothek ohne externe Dependency, Public-/Secret-Grenzen sowie die Bash-/PowerShell-Regeln aus R-013 ab. / The test contract covers every named positive and fail-closed case, using temporary projections for additional cases and proving no repository writes. Security review covers SSDF, CWE, safe Python boundary handling, dependency-free standard-library use, public/secret boundaries, and the paired shell rules.
- **Begruendung / Rationale**: Exakte Snapshot-Struktur allein beweist weder Lifecycle-Exklusivitaet, aktive Zustandsqualifikation, Review-Peer-Ausfall noch sichere/no-write Ausfuehrung. Temporaere Projektionen vermeiden redundante getrackte Fixtures und halten die Allowlist geschlossen. / Snapshot structure alone does not prove lifecycle exclusivity, active-state qualification, review-peer failure, or secure no-write execution. Temporary projections avoid redundant tracked fixtures and keep the allowlist closed.
- **Verworfene Alternative / Alternative considered**: Nur Happy Path und sechs alte Negativfaelle pruefen, jeden neuen Fall als getrackte Fixture speichern oder Produkt-/Runtime-Security-Scope erfinden. / Testing only the happy path and six prior negatives, tracking every projection, or inventing product/runtime security scope.

### R-015: Saubere Ein-Commit-Projektion fuer den Statistik-Renderer / Clean one-commit projection for the statistics renderer

- **Entscheidung / Decision**: T055 bis T058 verwenden einen mit `mktemp` erzeugten, explizit validierten temporaeren Git-Worktree und eine lokale Wegwerf-Branch von exakt `HEAD` des Feature-Branches. Die Projektion uebernimmt nur die zu diesem Zeitpunkt wirklich vorhandenen geaenderten oder beabsichtigten untracked Kandidatenpfade aus `normal-feature-candidate`; Runtime-Logs/-Ergebnisse, Caches, fremde Pfade und `docs/project-statistics.md` selbst bleiben Projektionsinput-`N/A`. Nach byteweisem Pfad-/Hash-/Diff-Abgleich entsteht nur im temporaeren Index genau ein lokaler Projektionscommit. Der kanonische Bash-Renderer und beide Check-only-Peers muessen dort bestehen; nur die erzeugten Ledger-Bytes werden in den realen Worktree kopiert. Worktree und Branch werden anschliessend ueber die zuvor gebundenen expliziten Ziele entfernt und ihr Fehlen wird belegt. / T055 through T058 use an explicitly validated `mktemp` worktree and disposable local branch at the exact feature `HEAD`. Only currently existing changed or intended-untracked paths from the accepted candidate transaction are copied; runtime artefacts, caches, unrelated paths, and the statistics ledger itself are excluded as projection input. Exactly one temporary projection commit is created after byte, inventory, hash, and diff reconciliation. The canonical renderer and both check-only peers pass there, only generated ledger bytes return, and the exact temporary worktree and ref are then removed and proven absent.
- **Begruendung / Rationale**: Der Schreib-Renderer verlangt einen sauberen Worktree. Methodik v2 liest aktuelle Git-getrackte Textdateien und Nicht-Merge-Commits und schliesst `docs/project-statistics.md`, `STATS.md` und Binaerdaten aus. Eine Projektion vom identischen Basis-HEAD mit genau einem Commit bildet deshalb die spaetere Ein-Commit-Kandidatenform ab, ohne den realen Branch, dessen Index oder History zu veraendern. Die realen Check-only-Oberflaechen sind zwar dirty-worktree-faehig, sehen aber bis zum echten Commit weder untracked Kandidatenpfade noch den Projektionscommit und muessen daher identisch `DRIFT` statt eines falschen `CURRENT` melden. Jede spaetere Kandidaten-, Evidence-, Task-, State-, Pfad- oder History-Aenderung erzwingt vor T071 eine erneute Projektion und Regeneration; ein realer `CURRENT`-Pass ist erst am entsprechenden sauberen Git-Zustand aussagekraeftig. / The writer requires a clean tree. Methodology v2 consumes tracked text and non-merge history while excluding the ledger, `STATS.md`, and binaries. The one-commit projection therefore models the later candidate without touching the real branch or index. Real check-only is supported but must report matching `DRIFT` until the real commit because untracked candidate files and the projection commit are absent there. Any later candidate or history drift requires regeneration before T071.
- **Verworfene Alternative / Alternative considered**: Vorzeitiges Staging oder Commit auf `002-portfolio-ownership`, Stash, Renderer- oder Methodiklockerung, alternative Git-Metadaten, Aufnahme synthetischer Pfade, Kopieren des Ledgers als Projektionsinput, Publish des Projektionscommits oder breites rekursives Aufraeumen. / Premature staging or commit on the feature branch, stash, renderer/methodology weakening, alternate Git metadata, synthetic paths, using the ledger as projection input, publishing the projection commit, or broad recursive cleanup.

### R-016: Nicht-rekursiver Statistik-Head nach dem normalen Kandidaten / Non-recursive statistics head after the normal candidate

- **Entscheidung / Decision**: Die T055-bis-T058-Projektion bleibt wahrheitsgemaesse historische Evidence. Der unpublizierte provisorische lokale Kandidat `7b99227045deb8cc34e0062db09eb4f6dd134501` bleibt ebenfalls erhalten und darf fuer die begrenzten Planungs-/Task-/Evidence-/State-/Allowlist-Korrekturen hoechstens einmal lokal amended werden. Das Amend muss Subject `docs: establish portfolio ownership baseline`, vorgeschriebenen Trailer und die vollstaendige 35-Pfad-Transaktion bewahren und danach alle betroffenen Gates neu bestehen. Erst vom eingefrorenen amended Normal-Head oder seiner bytegenauen sauberen Projektion rendert der unveraenderte kanonische Renderer den finalen Ledger. Danach entsteht genau ein weiterer lokaler Commit aus ausschliesslich `docs/project-statistics.md` mit Trailer. Dieser Statistik-Head ist der einzige Feature-/PR-Head; beide realen Check-only-Peers muessen dort `CURRENT`/`0` melden. / Preserve the historical projection and provisional commit, permit at most one reviewed local amend within the unchanged 35-path normal transaction, then render from the frozen amended normal head and create exactly one ledger-only synchronization commit as the sole final feature/PR head.
- **Begruendung / Rationale**: `docs/project-statistics.md` kann seine eigenen Source-SHA-Bytes nicht an denselben Commit binden, der diese Bytes einfuehrt. Methodik v2 zaehlt Git-getrackten Text und Bruttoaenderungen aus Nicht-Merge-Commits, schliesst aber `docs/project-statistics.md`, `STATS.md`, Binaerdaten und damit einen reinen Ledger-Commit aus. Der Ledger-only-Commit veraendert daher die Methodikquelle nicht: Er synchronisiert nur die generierte Darstellung mit dem bereits gefrorenen normalen Head. Das beseitigt die aktuelle identische reale `DRIFT`/`1`-Disposition (`3e1d9d5ccd98` statt `7b99227045de`, 222411 Textzeilen) ohne Rekursion, Renderer-Aenderung oder Scope-Zuwachs. / The ledger cannot self-bind the commit containing its own source-SHA bytes. Because methodology v2 excludes the ledger and ledger-only commits from counted input, one causal ledger-only commit synchronizes generated output without recursion or scope expansion.
- **Marker- und State-Grenze / Marker and state boundary**: T069 bis T072 werden als atomare Amend-Postconditions behandelt; ihre im amended Kandidaten enthaltenen Marker gelten nur bei vollstaendig bestandenem Amend. T073 darf erst auf dem Statistik-Head passieren und bleibt bis zum spaeteren kausalen Closeout ungecheckt, damit weder der reine Statistikcommit noch ein dritter Pre-Push-Commit durch Task-/State-Mutationen verbreitert wird. Runner-owned State nach der Publikationsgrenze folgt dem akzeptierten Closeout-Vertrag. / T069-T072 are atomic amend postconditions; T073 passes only on the statistics head and its later marker/state persistence follows causal closeout, avoiding a broadened or third pre-push commit.
- **Verworfene Alternative / Alternatives considered**: Den Ledger im normalen Commit an dessen noch unbekannten eigenen SHA binden; `7b992270` publizieren und `DRIFT` umdeuten; Renderer oder Methodik veraendern; den Statistikcommit um Tasks/State/Evidence verbreitern; mehr als ein Remediation-Amend oder mehr als einen vorab autorisierten Statistikcommit erzeugen; den provisorischen Commit verwerfen; technische Gates oder Windows-Evidence bypassen. / Self-binding the normal commit, reinterpreting drift, changing renderer/methodology, broadening the ledger commit, silently authorising further commits, discarding provisional evidence, or bypassing technical gates.

## Aufgeloeste Unbekannte / Resolved unknowns

Es verbleiben keine `NEEDS CLARIFICATION`-Marker. Die fehlende AOC-Zeile im gemeinsamen Level-2-Register wird fuer den dokumentarischen Scope mit eng begrenztem feature-lokalem Validierungstooling als begruendetes `N/A` behandelt; daraus wird keine Produkt-Runtime abgeleitet und keine Governance-Datei geaendert. / *No `NEEDS CLARIFICATION` marker remains. The missing AOC row is justified N/A for documentary scope plus bounded feature-local validation tooling; no product runtime is inferred and no governance file is changed.*
